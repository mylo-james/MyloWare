## Example environment configuration for local development
#
# NOTE:
# - Do NOT use these values in staging or production.
# - CI runs a check to ensure this file does not suggest
#   production/staging defaults (e.g., no real API keys).
#
# ⚠️  IMPORTANT: Provider Mode
# - PROVIDERS_MODE defaults to "mock" (safe, free, fast)
# - NEVER set PROVIDERS_MODE=live in this file
# - Tests ALWAYS use mock mode (enforced by pytest fixtures)
# - Only set PROVIDERS_MODE=live manually for production runs
# - Setting it to "live" will cost real money on kie.ai, Shotstack, etc.

API_KEY=dev-local-api-key
DB_URL=postgresql+psycopg://postgres:postgres@localhost:5432/myloware

# PROVIDERS_MODE=mock  # ← This is the default, no need to set it
# PROVIDERS_MODE=live  # ← NEVER set this here! Tests will cost $$$

KIEAI_API_KEY=dev-kieai
KIEAI_SIGNING_SECRET=dev-kieai-signing-secret
SHOTSTACK_API_KEY=dev-shotstack
UPLOAD_POST_API_KEY=dev-upload-post
UPLOAD_POST_SIGNING_SECRET=dev-upload-post-signing-secret
HITL_SECRET=your-hitl-secret-here-generate-secure-random-string

LANGSMITH_API_KEY=
SENTRY_DSN=
REDIS_URL=redis://localhost:6379/0
