### Install and Run Git Server (Python with pip)

Source: https://modelcontextprotocol.io/examples

Illustrates installing and running the Git MCP server using pip and the Python module execution. This server enables Git repository operations.

```bash
pip install mcp-server-git
python -m mcp_server_git
```

--------------------------------

### Run Git Server (Python with uvx)

Source: https://modelcontextprotocol.io/examples

Shows how to execute the Git MCP server using uvx, a Python version manager and package installer. This server facilitates reading, searching, and manipulating Git repositories.

```bash
uvx mcp-server-git
```

--------------------------------

### Install uv and Set Up Python Project (Windows)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Installs the `uv` package manager and sets up a new Python project environment on Windows. This includes creating a project directory, initializing a virtual environment, and installing necessary dependencies like `mcp[cli]` and `httpx`.

```PowerShell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

# Create a new directory for our project
uv init weather
cd weather

# Create virtual environment and activate it
uv venv
.venv\Scripts\activate

# Install dependencies
uv add mcp[cli] httpx

# Create our server file
new-item weather.py
```

--------------------------------

### Install uv and Set Up Python Project (macOS/Linux)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Installs the `uv` package manager and sets up a new Python project environment. This includes creating a project directory, initializing a virtual environment, and installing necessary dependencies like `mcp[cli]` and `httpx`.

```Bash
curl -LsSf https://astral.sh/uv/install.sh | sh

# Create a new directory for our project
uv init weather
cd weather

# Create virtual environment and activate it
uv venv
source .venv/bin/activate

# Install dependencies
uv add "mcp[cli]" httpx

# Create our server file
touch weather.py
```

--------------------------------

### Inspect NPM or PyPi Servers with MCP Inspector

Source: https://modelcontextprotocol.io/docs/tools/inspector

Illustrates how to use the MCP Inspector to start server packages from NPM or PyPi. It provides examples for both package managers.

```bash
npx -y @modelcontextprotocol/inspector npx <package-name> <args>
# For example
npx -y @modelcontextprotocol/inspector npx @modelcontextprotocol/server-filesystem /Users/username/Desktop
```

```bash
npx @modelcontextprotocol/inspector uvx <package-name> <args>
# For example
npx @modelcontextprotocol/inspector uvx mcp-server-git --repository ~/code/mcp/servers.git
```

--------------------------------

### Initialize Node.js Project and Install Dependencies (Bash)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Sets up a new Node.js project, initializes npm, and installs necessary SDKs and development dependencies for the ModelContextProtocol client.

```bash
# Create project directory
mkdir mcp-client-typescript
cd mcp-client-typescript

# Initialize npm project
npm init -y

# Install dependencies
npm install @anthropic-ai/sdk @modelcontextprotocol/sdk dotenv

# Install dev dependencies
npm install -D @types/node typescript

# Create source file
touch index.ts
```

--------------------------------

### Node.js Project Setup for MCP Weather Server (macOS/Linux)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Provides bash commands to set up a new Node.js project for an MCP weather server. This includes creating a directory, initializing npm, installing necessary dependencies like `@modelcontextprotocol/sdk` and `zod`, and setting up the project structure.

```bash
# Create a new directory for our project
mkdir weather
cd weather

# Initialize a new npm project
npm init -y

# Install dependencies
npm install @modelcontextprotocol/sdk zod@3
npm install -D @types/node typescript

# Create our files
mkdir src
touch src/index.ts
```

--------------------------------

### Install and Run MCP Inspector

Source: https://modelcontextprotocol.io/docs/tools/inspector

Demonstrates how to run the MCP Inspector directly via npx without prior installation. It shows the basic command structure with optional arguments.

```bash
npx @modelcontextprotocol/inspector <command>
```

```bash
npx @modelcontextprotocol/inspector <command> <arg1> <arg2>
```

--------------------------------

### Clone Spring AI Examples Repository

Source: https://modelcontextprotocol.io/docs/develop/build-client

Clones the official Spring AI examples repository and navigates into the specific directory for the Brave Chatbot example.

```bash
git clone https://github.com/spring-projects/spring-ai-examples.git
cd model-context-protocol/brave-chatbot
```

--------------------------------

### Node.js Project Setup for MCP Weather Server (Windows)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Provides PowerShell commands to set up a new Node.js project for an MCP weather server on Windows. This includes creating a directory, initializing npm, installing dependencies, and creating the project structure.

```powershell
# Create a new directory for our project
md weather
cd weather

# Initialize a new npm project
npm init -y

# Install dependencies
npm install @modelcontextprotocol/sdk zod@3
npm install -D @types/node typescript

# Create our files
md src
new-item src\index.ts
```

--------------------------------

### Initialize Node.js Project and Install Dependencies (PowerShell)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Sets up a new Node.js project on Windows, initializes npm, and installs necessary SDKs and development dependencies for the ModelContextProtocol client.

```powershell
# Create project directory
md mcp-client-typescript
cd mcp-client-typescript

# Initialize npm project
npm init -y

# Install dependencies
npm install @anthropic-ai/sdk @modelcontextprotocol/sdk dotenv
```

--------------------------------

### MCP Client Path Examples (Bash)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Provides examples of correct command-line syntax for running the MCP client, demonstrating the use of relative paths, absolute paths, and handling Windows-style paths with either forward or escaped backslashes.

```bash
# Relative path
node build/index.js ./server/build/index.js

# Absolute path
node build/index.js /Users/username/projects/mcp-server/build/index.js

# Windows path (either format works)
node build/index.js C:/projects/mcp-server/build/index.js
node build/index.js C:\\projects\\mcp-server\\build\\index.js
```

--------------------------------

### Set up C# Weather Server Environment

Source: https://modelcontextprotocol.io/docs/develop/build-server

Steps to set up a C# project for an MCP weather server, including installing the .NET SDK and creating a new console application.

```bash
# Create a new directory for our project
mkdir weather
cd weather
# Initialize a new C# project
dotnet new console
```

--------------------------------

### Rust Main Function Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

A simple Rust program demonstrating a main function that prints 'Hello world!' to the console.

```rust
fn main() {
    println!("Hello world!");
}
```

--------------------------------

### Set up C# Weather Server Environment (Windows)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Steps to set up a C# project for an MCP weather server on Windows, including installing the .NET SDK and creating a new console application.

```powershell
# Create a new directory for our project
mkdir weather
cd weather
# Initialize a new C# project
dotnet new console
```

--------------------------------

### Configure MCP Filesystem Server

Source: https://modelcontextprotocol.io/examples

This configuration sets up an MCP filesystem server using npx, allowing access to specified files. It includes the command, arguments, and the path to allowed files.

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/path/to/allowed/files"
      ]
    }
  }
}
```

--------------------------------

### Configure MCP Memory Server

Source: https://modelcontextprotocol.io/examples

This configuration sets up an MCP memory server using npx. It specifies the command to run and its arguments.

```json
{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    }
  }
}
```

--------------------------------

### Install Project Dependencies

Source: https://modelcontextprotocol.io/docs/develop/build-client

Installs the necessary Node.js packages for Anthropic and the Model Context Protocol SDK, along with development dependencies for TypeScript.

```bash
npm install @anthropic-ai/sdk @modelcontextprotocol/sdk dotenv

# Install dev dependencies
npm install -D @types/node typescript
```

--------------------------------

### Resource Template Examples in JSON

Source: https://modelcontextprotocol.io/docs/learn/server-concepts

Examples of resource templates used for flexible queries, including URI templates, names, titles, descriptions, and MIME types. These templates allow for dynamic data retrieval based on parameters.

```json
{
  "uriTemplate": "weather://forecast/{city}/{date}",
  "name": "weather-forecast",
  "title": "Weather Forecast",
  "description": "Get weather forecast for any city and date",
  "mimeType": "application/json"
}

{
  "uriTemplate": "travel://flights/{origin}/{destination}",
  "name": "flight-search",
  "title": "Flight Search",
  "description": "Search available flights between cities",
  "mimeType": "application/json"
}
```

--------------------------------

### Run MCP Server (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Initializes and runs the MCP server using the 'stdio' transport. This is the main entry point for starting the weather server.

```Python
if __name__ == "__main__":
    # Initialize and run the server
    mcp.run(transport='stdio')
```

--------------------------------

### Install npx Package Manager

Source: https://modelcontextprotocol.io/docs/develop/build-client

Installs the npx package manager globally, which is required for executing Node.js packages like the Brave Search MCP Server.

```bash
npm install -g npx
```

--------------------------------

### Configure MCP GitHub Server with Token

Source: https://modelcontextprotocol.io/examples

This configuration sets up an MCP GitHub server using npx and requires a GitHub Personal Access Token for authentication, provided via environment variables.

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "<YOUR_TOKEN>"
      }
    }
  }
}
```

--------------------------------

### Resource with Annotations Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

An example JSON structure for a resource that includes annotations for audience, priority, and last modified timestamp.

```json
{
  "uri": "file:///project/README.md",
  "name": "README.md",
  "title": "Project Documentation",
  "mimeType": "text/markdown",
  "annotations": {
    "audience": ["user"],
    "priority": 0.8,
    "lastModified": "2025-01-12T15:00:58Z"
  }
}
```

--------------------------------

### Example MCP HTTP GET Request with Authorization

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

An example of an HTTP GET request made by an MCP client, including the Host header and the Authorization header with a Bearer token.

```http
GET /mcp HTTP/1.1
Host: mcp.example.com
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

--------------------------------

### MCP Client Server Path Troubleshooting Examples

Source: https://modelcontextprotocol.io/docs/develop/build-client

Illustrates correct path formats for running the MCP client against a server, including relative paths, absolute paths, and Windows-specific path formats.

```bash
# Relative path
java -jar build/libs/client.jar ./server/build/libs/server.jar

# Absolute path
java -jar build/libs/client.jar /Users/username/projects/mcp-server/build/libs/server.jar

# Windows path (either format works)
java -jar build/libs/client.jar C:/projects/mcp-server/build/libs/server.jar
java -jar build/libs/client.jar C:\projects\mcp-server\build\libs\server.jar
```

--------------------------------

### Text Content Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Demonstrates the basic structure for returning plain text content from a tool. This is the simplest form of content.

```json
{
  "type": "text",
  "text": "Tool result text"
}
```

--------------------------------

### Rust AgentAI MCP Server Integration Example

Source: https://modelcontextprotocol.io/clients

Demonstrates how to integrate MCP Servers within the AgentAI Rust library. This example showcases setting up and utilizing MCP server connections for AI agent functionalities, supporting various LLM APIs.

```rust
https://github.com/AdamStrojek/rust-agentai/blob/master/examples/tools_mcp.rs
```

--------------------------------

### JSON RPC Initialize Request Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle

An example of a JSON RPC 'initialize' request sent by a client to a server, including protocol version, client capabilities, and implementation information.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "initialize",
  "params": {
    "protocolVersion": "2024-11-05",
    "capabilities": {
      "roots": {
        "listChanged": true
      },
      "sampling": {},
      "elicitation": {}
    },
    "clientInfo": {
      "name": "ExampleClient",
      "title": "Example Client Display Name",
      "version": "1.0.0"
    }
  }
}
```

--------------------------------

### Initializing MCP Server Instance (TypeScript)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Demonstrates how to import necessary classes from the `@modelcontextprotocol/sdk` and initialize an `McpServer` instance. This includes setting server metadata like name, version, and capabilities, which are essential for MCP server setup.

```typescript
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import { z } from "zod";

const NWS_API_BASE = "https://api.weather.gov";
const USER_AGENT = "weather-app/1.0";

// Create server instance
const server = new McpServer({
  name: "weather",
  version: "1.0.0",
  capabilities: {
    resources: {},
    tools: {},
  },
});
```

--------------------------------

### JSON RPC Initialize Response Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle

An example of a JSON RPC 'initialize' response from a server to a client, detailing server capabilities, information, and optional instructions.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "protocolVersion": "2024-11-05",
    "capabilities": {
      "logging": {},
      "prompts": {
        "listChanged": true
      },
      "resources": {
        "subscribe": true,
        "listChanged": true
      },
      "tools": {
        "listChanged": true
      }
    },
    "serverInfo": {
      "name": "ExampleServer",
      "title": "Example Server Display Name",
      "version": "1.0.0"
    },
    "instructions": "Optional instructions for the client"
  }
}
```

--------------------------------

### Resource Text Content Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

An example JSON structure representing a text resource. It includes URI, name, title, MIME type, and the text content itself.

```json
{
  "uri": "file:///example.txt",
  "name": "example.txt",
  "title": "Example Text File",
  "mimeType": "text/plain",
  "text": "Resource content"
}
```

--------------------------------

### Example Rust Code Snippet

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

A basic Rust function demonstrating a 'Hello world!' output, representative of code that might be exposed as a resource.

```rust
fn main() {
    println!("Hello world!");
}
```

--------------------------------

### Resource Link Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Demonstrates how to provide a link to an external resource, including its URI, name, description, MIME type, and optional annotations.

```json
{
  "type": "resource_link",
  "uri": "file:///project/src/main.rs",
  "name": "main.rs",
  "description": "Primary application entry point",
  "mimeType": "text/x-rust",
  "annotations": {
    "audience": ["assistant"],
    "priority": 0.9
  }
}
```

--------------------------------

### Install NPM Globally

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

Command to install NPM globally, which may be required for the `npx` command to function correctly. This ensures that Node Package Manager is available system-wide.

```bash
npm install -g npm
```

--------------------------------

### Run Memory Server (TypeScript)

Source: https://modelcontextprotocol.io/examples

Demonstrates how to run the Memory MCP server using npx, a command-line tool for executing Node.js packages. This server provides knowledge graph-based persistent memory.

```bash
npx -y @modelcontextprotocol/server-memory
```

--------------------------------

### Audio Content Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Shows the structure for returning audio data, specifying the MIME type and base64 encoded audio data.

```json
{
  "type": "audio",
  "data": "base64-encoded-audio-data",
  "mimeType": "audio/wav"
}
```

--------------------------------

### Root Structure Example (JSON)

Source: https://modelcontextprotocol.io/docs/learn/client-concepts

Demonstrates the structure of a 'root' object used to define filesystem boundaries for server operations. It includes a URI for the directory and a descriptive name.

```json
{
  "uri": "file:///Users/agent/travel-planning",
  "name": "Travel Planning Workspace"
}
```

--------------------------------

### Resource Binary Content Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

An example JSON structure representing a binary resource. It includes URI, name, title, MIME type, and base64-encoded binary data.

```json
{
  "uri": "file:///example.png",
  "name": "example.png",
  "title": "Example Image",
  "mimeType": "image/png",
  "blob": "base64-encoded-data"
}
```

--------------------------------

### Run MCP Client with Different Server Types

Source: https://modelcontextprotocol.io/docs/develop/build-client

Command examples to build the Kotlin client and run it against various server implementations (JVM, Python, Node.js). Assumes the client JAR is built and available.

```bash
./gradlew build

# Run the client
java -jar build/libs/<your-jar-name>.jar path/to/server.jar # jvm server
java -jar build/libs/<your-jar-name>.jar path/to/server.py # python server
java -jar build/libs/<your-jar-name>.jar path/to/build/index.js # node server
```

--------------------------------

### Install Amazon Q CLI using Homebrew

Source: https://modelcontextprotocol.io/clients

This command installs the Amazon Q CLI, an open-source agentic coding assistant for terminals, using the Homebrew package manager. It enables full support for MCP servers and terminal-based AWS resource management.

```bash
brew install amazon-q
```

--------------------------------

### Verify Node.js Installation

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

This command checks if Node.js is installed on your system and displays its version. It's a prerequisite for running MCP servers that rely on Node.js.

```bash
node --version
```

--------------------------------

### Error Response Example (Method Not Found)

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/roots

An example of a JSON-RPC error response when the client does not support the roots capability, using code -32601.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32601,
    "message": "Roots not supported",
    "data": {
      "reason": "Client does not have roots capability"
    }
  }
}
```

--------------------------------

### Configure Filesystem Server Access

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

This snippet shows the command used to start the Filesystem Server via npx, including automatic confirmation and the package name. It also specifies the directories the server is permitted to access, which can be customized.

```json
{
  "filesystem": {
    "command": "npx",
    "args": [
      "-y",
      "@modelcontextprotocol/server-filesystem",
      "/path/to/your/directory",
      "/another/accessible/directory"
    ]
  }
}
```

--------------------------------

### Build and Run Application (Alternative)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Provides an alternative method to build and run the application using Maven. This involves cleaning the project, installing dependencies, and then running the compiled JAR file.

```bash
./mvnw clean install
java -jar ./target/ai-mcp-brave-chatbot-0.0.1-SNAPSHOT.jar
```

--------------------------------

### Test Weather Server with Claude for Desktop Commands

Source: https://modelcontextprotocol.io/docs/develop/build-server

This snippet provides example natural language commands to test if a 'weather' server's tools are correctly integrated and recognized by Claude for Desktop. It assumes the tools have been exposed and the application is configured to find them.

```Natural Language
What's the weather in Sacramento?
```

```Natural Language
What are the active weather alerts in Texas?
```

--------------------------------

### Main Entry Point (Kotlin)

Source: https://modelcontextprotocol.io/docs/develop/build-client

The main function that serves as the entry point for the application. It is responsible for starting the chat loop within a coroutine scope using `runBlocking`.

```kotlin
fun main(args: Array<String>) = runBlocking {
```

--------------------------------

### Bash Command to Run Weather Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

Executes the .NET server application. This command starts the server, making it available to listen for incoming requests on standard input/output.

```bash
dotnet run
```

--------------------------------

### Configure MCP Servers in Claude for Desktop

Source: https://modelcontextprotocol.io/docs/develop/build-server

Example JSON snippet for the Claude for Desktop configuration file, showing how to add an MCP server named 'spring-ai-mcp-weather'. It specifies the command and arguments to run the server, including enabling stdio mode.

```json
{
  "mcpServers": {
    "spring-ai-mcp-weather": {
      "command": "java",
      "args": [
        "-Dspring.ai.mcp.server.stdio=true",

```

--------------------------------

### Initialize MCP Client Environment (Bash)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Sets up a new Python project using 'uv', creates and activates a virtual environment, installs necessary packages (mcp, anthropic, python-dotenv), and prepares the project structure by removing boilerplate and creating a client file.

```bash
# Create project directory
uv init mcp-client
cd mcp-client

# Create virtual environment
uv venv

# Activate virtual environment
# On Windows:
.venv\Scripts\activate
# On Unix or macOS:
source .venv/bin/activate

# Install required packages
uv add mcp anthropic python-dotenv

# Remove boilerplate files
# On Windows:
del main.py
# On Unix or macOS:
rm main.py

# Create our main file
touch client.py
```

--------------------------------

### JSON RPC Initialized Notification Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle

An example of a JSON RPC 'initialized' notification sent by a client to a server after successful initialization, signaling readiness for normal operations.

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/initialized"
}
```

--------------------------------

### Client Initiating SSE Stream via HTTP GET

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

Demonstrates how a client can initiate a Server-Sent Events (SSE) stream by sending an HTTP GET request to the MCP endpoint, specifying the desired content type.

```HTTP
GET /mcp-endpoint HTTP/1.1
Host: your-server.com
Accept: text/event-stream


```

--------------------------------

### Run Spring Boot Application with Maven

Source: https://modelcontextprotocol.io/docs/develop/build-client

Executes the Spring Boot application using Maven's spring-boot:run goal. This is a convenient way to start the application during development.

```bash
./mvnw spring-boot:run
```

--------------------------------

### MCP Protocol Version Header Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

Demonstrates the 'MCP-Protocol-Version' header used by clients to specify the protocol version to the MCP server.

```HTTP
Request Header:
MCP-Protocol-Version: 2025-06-18
```

--------------------------------

### Elicitation Request Input Example

Source: https://modelcontextprotocol.io/docs/learn/client-concepts

Demonstrates the structure of a request to elicit specific user input, including message and a JSON schema for validation.

```typescript
{
  method: "elicitation/requestInput",
  params: {
    message: "Please confirm your Barcelona vacation booking details:",
    schema: {
      type: "object",
      properties: {
        confirmBooking: {
          type: "boolean",
          description: "Confirm the booking (Flights + Hotel = $3,000)"
        },
        seatPreference: {
          type: "string",
          enum: ["window", "aisle", "no preference"],
          description: "Preferred seat type for flights"
        },
        roomType: {
          type: "string",
          enum: ["sea view", "city view", "garden view"],
          description: "Preferred room type at hotel"
        },
        travelInsurance: {
          type: "boolean",
          default: false,
          description: "Add travel insurance ($150)"
        }
      },
      required: ["confirmBooking"]
    }
  }
}
```

--------------------------------

### Inspect Locally Developed Servers with MCP Inspector

Source: https://modelcontextprotocol.io/docs/tools/inspector

Shows how to inspect locally developed servers using the MCP Inspector, supporting both TypeScript (Node.js) and Python environments.

```bash
npx @modelcontextprotocol/inspector node path/to/server/index.js args...
```

```bash
npx @modelcontextprotocol/inspector \
  uv \
  --directory path/to/server \
  run \
  package-name \
  args...
```

--------------------------------

### Image Content Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Illustrates how to return image data, including base64 encoded data and MIME type. It also shows optional annotations for audience and priority.

```json
{
  "type": "image",
  "data": "base64-encoded-data",
  "mimeType": "image/png",
  "annotations": {
    "audience": ["user"],
    "priority": 0.9
  }
}
```

--------------------------------

### Running the MCP Client (Bash)

Source: https://modelcontextprotocol.io/docs/develop/build-client

These commands illustrate how to build the TypeScript client using 'npm run build' and then execute it against an MCP server. It shows examples for both Python and Node.js server scripts, emphasizing the correct path arguments.

```bash
# Build TypeScript
npm run build

# Run the client
node build/index.js path/to/server.py # python server
node build/index.js path/to/build/index.js # node server
```

--------------------------------

### Initialize Kotlin Project with Gradle

Source: https://modelcontextprotocol.io/docs/develop/build-client

Commands to create and initialize a new Kotlin project using Gradle on macOS/Linux and Windows. This includes creating a project directory and running the `gradle init` command to set up the project structure.

```bash
# Create a new directory for our project
mkdir kotlin-mcp-client
cd kotlin-mcp-client

# Initialize a new kotlin project
gradle init
```

```powershell
# Create a new directory for our project
md kotlin-mcp-client
cd kotlin-mcp-client
# Initialize a new kotlin project
grade init
```

--------------------------------

### Server Initiating SSE Stream

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

Example of a server initiating an SSE stream in response to a client's request, setting the appropriate Content-Type header.

```HTTP
HTTP/1.1 200 OK
Content-Type: text/event-stream


```

--------------------------------

### Elicitation Response Actions Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

Illustrates the JSON structure for elicitation responses, specifying the action (accept, decline, cancel) and the associated content.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "action": "accept", // or "decline" or "cancel"
    "content": {
      "propertyName": "value",
      "anotherProperty": 42
    }
  }
}
```

--------------------------------

### Boolean Schema Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

Defines a schema for boolean types, including properties for title, description, and a default value.

```json
{
  "type": "boolean",
  "title": "Display Name",
  "description": "Description text",
  "default": false
}
```

--------------------------------

### Sampling Request Parameters

Source: https://modelcontextprotocol.io/docs/learn/client-concepts

Example of parameters for a sampling request, including messages, model preferences (cost, speed, intelligence priorities), system prompt, and maximum tokens.

```typescript
{
  messages: [
    {
      role: "user",
      content: "Analyze these flight options and recommend the best choice:\n" +
               "[47 flights with prices, times, airlines, and layovers]\n" +
               "User preferences: morning departure, max 1 layover"
    }
  ],
  modelPreferences: {
    hints: [{
      name: "claude-3-5-sonnet"  // Suggested model
    }],
    costPriority: 0.3,      // Less concerned about API cost
    speedPriority: 0.2,     // Can wait for thorough analysis
    intelligencePriority: 0.9  // Need complex trade-off evaluation
  },
  systemPrompt: "You are a travel expert helping users find the best flights based on their preferences",
  maxTokens: 1500
}
```

--------------------------------

### Run MCP Client with Different Server Types

Source: https://modelcontextprotocol.io/docs/develop/build-client

Execute the MCP client by specifying the path to the server's project file. Supports .NET (C#), Python, and Node.js servers. The client initiates a connection and starts an interactive chat.

```bash
dotnet run -- path/to/server.csproj # dotnet server
dotnet run -- path/to/server.py # python server
dotnet run -- path/to/server.js # node server
```

--------------------------------

### Initialize Kotlin Project with Gradle

Source: https://modelcontextprotocol.io/docs/develop/build-server

Commands to create and initialize a new Kotlin application project using Gradle on macOS/Linux and Windows.

```bash
# Create a new directory for our project
mkdir weather
cd weather

# Initialize a new kotlin project
gradle init

```

```powershell
# Create a new directory for our project
md weather
cd weather

# Initialize a new kotlin project
gradle init

```

--------------------------------

### Model Selection with Hints and Priorities

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/sampling

Demonstrates how to use 'hints' and 'priorities' to guide LLM model selection. Hints are treated as flexible substrings for matching model names, and priorities influence the selection based on cost, speed, and intelligence.

```json
{
  "hints": [
    { "name": "claude-3-sonnet" },
    { "name": "claude" }
  ],
  "costPriority": 0.3,
  "speedPriority": 0.8,
  "intelligencePriority": 0.5
}
```

--------------------------------

### JSON-RPC Protocol Error Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Illustrates a JSON-RPC protocol error, specifically for an invalid method call (e.g., unknown tool), with an error code and message.

```json
{
  "jsonrpc": "2.0",
  "id": 3,
  "error": {
    "code": -32602,
    "message": "Unknown tool: invalid_tool_name"
  }
}
```

--------------------------------

### Number Schema Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

Defines a schema for number types (or integer), including properties for title, description, minimum, and maximum values.

```json
{
  "type": "number", // or "integer"
  "title": "Display Name",
  "description": "Description text",
  "minimum": 0,
  "maximum": 100
}
```

--------------------------------

### Multiple Roots Data Type Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/roots

Multiple roots can be provided as an array of root objects, each with a 'uri' and an optional 'name'.

```json
[
  {
    "uri": "file:///home/user/repos/frontend",
    "name": "Frontend Repository"
  },
  {
    "uri": "file:///home/user/repos/backend",
    "name": "Backend Repository"
  }
]
```

--------------------------------

### Root Data Type Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/roots

A single root is defined by a 'uri' and an optional 'name'. The 'uri' must be a 'file://' URI.

```json
{
  "uri": "file:///home/user/projects/myproject",
  "name": "My Project"
}
```

--------------------------------

### Valid Tool Response with Structured Content

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

An example of a valid JSON-RPC response for the 'get_weather_data' tool, including structured content that conforms to the defined output schema.

```json
{
  "jsonrpc": "2.0",
  "id": 5,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "{\"temperature\": 22.5, \"conditions\": \"Partly cloudy\", \"humidity\": 65}"
      }
    ],
    "structuredContent": {
      "temperature": 22.5,
      "conditions": "Partly cloudy",
      "humidity": 65
    }
  }
}
```

--------------------------------

### SSE Event with ID

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

An example of a Server-Sent Event (SSE) message that includes a unique ID, used for stream resumability.

```SSE
id: unique-event-id-123
event: message
data: {"jsonrpc": "2.0", "result": "some_result"}


```

--------------------------------

### Configure APPDATA Environment Variable for Windows

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

Example JSON configuration for `claude_desktop_config.json` to explicitly set the APPDATA environment variable. This resolves issues where paths containing `${APPDATA}` might fail to load on Windows.

```json
{
  "brave-search": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-brave-search"],
    "env": {
      "APPDATA": "C:\\Users\\user\\AppData\\Roaming\\",
      "BRAVE_API_KEY": "..."
    }
  }
}
```

--------------------------------

### Main Entry Point for MCP Client (TypeScript)

Source: https://modelcontextprotocol.io/docs/develop/build-client

This is the main execution function that initializes the MCP client, connects to a specified server script, starts the interactive chat loop, and ensures cleanup operations are performed upon exit. It includes basic command-line argument validation.

```typescript
    async function main() {
      if (process.argv.length < 3) {
        console.log("Usage: node index.ts <path_to_server_script>");
        return;
      }
      const mcpClient = new MCPClient();
      try {
        await mcpClient.connectToServer(process.argv[2]);
        await mcpClient.chatLoop();
      } finally {
        await mcpClient.cleanup();
        process.exit(0);
      }
    }

    main();
```

--------------------------------

### MCP Architecture Diagram

Source: https://modelcontextprotocol.io/docs/learn/architecture

Illustrates the client-server architecture of the Model Context Protocol (MCP). It shows an MCP Host managing multiple MCP Clients, each establishing a one-to-one connection with an MCP Server. Examples of servers include Sentry, Filesystem, and Database.

```mermaid
graph TB
    subgraph "MCP Host (AI Application)"
        Client1["MCP Client 1"]
        Client2["MCP Client 2"]
        Client3["MCP Client 3"]
    end

    Server1["MCP Server 1<br/>(e.g., Sentry)"]
    Server2["MCP Server 2<br/>(e.g., Filesystem)"]
    Server3["MCP Server 3<br/>(e.g., Database)"]

    Client1 ---|"One-to-one<br/>connection"| Server1
    Client2 ---|"One-to-one<br/>connection"| Server2
    Client3 ---|"One-to-one<br/>connection"| Server3

    style Client1 fill:#e1f5fe
    style Client2 fill:#e1f5fe
    style Client3 fill:#e1f5fe
    style Server1 fill:#f3e5f5
    style Server2 fill:#f3e5f5
    style Server3 fill:#f3e5f5
```

--------------------------------

### String Schema Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

Defines a schema for string types, including properties like title, description, minLength, maxLength, and supported formats such as email, uri, date, and date-time.

```json
{
  "type": "string",
  "title": "Display Name",
  "description": "Description text",
  "minLength": 3,
  "maxLength": 50,
  "format": "email" // Supported: "email", "uri", "date", "date-time"
}
```

--------------------------------

### JSON-RPC Error Response Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

This snippet demonstrates a typical JSON-RPC error response structure for a 'Resource not found' scenario, including the error code, message, and relevant data.

```json
{
  "jsonrpc": "2.0",
  "id": 5,
  "error": {
    "code": -32002,
    "message": "Resource not found",
    "data": {
      "uri": "file:///nonexistent.txt"
    }
  }
}
```

--------------------------------

### Embedded Resource Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Illustrates embedding a resource directly within the response, including its URI, title, MIME type, text content, and annotations like lastModified.

```json
{
  "type": "resource",
  "resource": {
    "uri": "file:///project/src/main.rs",
    "title": "Project Rust Main File",
    "mimeType": "text/x-rust",
    "text": "fn main() {\n    println!(\"Hello world!\");\n}",
    "annotations": {
      "audience": ["user", "assistant"],
      "priority": 0.7,
      "lastModified": "2025-05-03T14:30:00Z"
    }
  }
}
```

--------------------------------

### MCP Session ID Header Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

Illustrates the use of the 'Mcp-Session-Id' header in HTTP requests and responses for managing stateful sessions in the Model Context Protocol.

```HTTP
Request Header:
Mcp-Session-Id: 1868a90c...

Response Header:
Mcp-Session-Id: 1868a90c...
```

--------------------------------

### MCP Prompt Get Response

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

The response to a 'prompts/get' request contains the prompt's description and a list of messages, typically including user-provided content formatted for the language model.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "description": "Code review prompt",
    "messages": [
      {
        "role": "user",
        "content": {
          "type": "text",
          "text": "Please review this Python code:\ndef hello():\n    print('world')"
        }
      }
    ]
  }
}
```

--------------------------------

### Get Weather Forecast (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Retrieves a weather forecast for a given latitude and longitude. It first finds the forecast endpoint using the NWS API and then fetches and formats the forecast for the next five periods.

```Python
@mcp.tool()
async def get_forecast(latitude: float, longitude: float) -> str:
    """Get weather forecast for a location.

    Args:
        latitude: Latitude of the location
        longitude: Longitude of the location
    """
    # First get the forecast grid endpoint
    points_url = f"{NWS_API_BASE}/points/{latitude},{longitude}"
    points_data = await make_nws_request(points_url)

    if not points_data:
        return "Unable to fetch forecast data for this location."

    # Get the forecast URL from the points response
    forecast_url = points_data["properties"]["forecast"]
    forecast_data = await make_nws_request(forecast_url)

    if not forecast_data:
        return "Unable to fetch detailed forecast."

    # Format the periods into a readable forecast
    periods = forecast_data["properties"]["periods"]
    forecasts = []
    for period in periods[:5]:  # Only show next 5 periods
        forecast = f"""
    {period['name']}:
    Temperature: {period['temperature']}°{period['temperatureUnit']}
    Wind: {period['windSpeed']} {period['windDirection']}
    Forecast: {period['detailedForecast']}
    """
        forecasts.append(forecast)

    return "\n---\n".join(forecasts)
```

--------------------------------

### Enum Schema Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

Defines a schema for enum types (string), including properties for title, description, allowed enum values, and corresponding enum names.

```json
{
  "type": "string",
  "title": "Display Name",
  "description": "Description text",
  "enum": ["option1", "option2", "option3"],
  "enumNames": ["Option 1", "Option 2", "Option 3"]
}
```

--------------------------------

### Create .NET Project and Add Dependencies

Source: https://modelcontextprotocol.io/docs/develop/build-client

These commands set up a new .NET console project and add the necessary NuGet packages for ModelContextProtocol, Anthropic SDK, and AI extensions.

```bash
dotnet new console -n QuickstartClient
cd QuickstartClient
dotnet add package ModelContextProtocol --prerelease
dotnet add package Anthropic.SDK
dotnet add package Microsoft.Extensions.Hosting
dotnet add package Microsoft.Extensions.AI
```

--------------------------------

### Set up API Key and Gitignore (Bash)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Demonstrates how to create a '.env' file to securely store the Anthropic API key and adds '.env' to the '.gitignore' file to prevent accidental commits.

```bash
# Create .env file
touch .env

# Add your key to the .env file:
# ANTHROPIC_API_KEY=<your key here>

# Add .env to your .gitignore:
echo ".env" >> .gitignore
```

--------------------------------

### Example Initialization Error

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle

This JSON snippet demonstrates a typical error response during initialization, specifically indicating an unsupported protocol version. It includes the JSON-RPC version, a request ID, and an error object with a code, message, and additional data detailing supported and requested versions.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32602,
    "message": "Unsupported protocol version",
    "data": {
      "supported": ["2024-11-05"],
      "requested": "1.0.0"
    }
  }
}
```

--------------------------------

### Connect to MCP Server and List Tools (Kotlin)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Establishes a connection to the MCP server using a specified script (e.g., .js, .py, .jar). It then lists the available tools from the server and prepares them for use with the LLM. This involves process creation, stream handling, and mapping server tool definitions to a client-side representation.

```kotlin
            val command = mutableListOf("node", serverScriptPath)
            if (!serverScriptPath.endsWith(".js")) {
                else -> throw IllegalArgumentException("Server script must be a .js, .py or .jar file")
            }
            add(serverScriptPath)
        }

        val process = ProcessBuilder(command).start()
        val transport = StdioClientTransport(
            input = process.inputStream.asSource().buffered(),
            output = process.outputStream.asSink().buffered()
        )

        mcp.connect(transport)

        val toolsResult = mcp.listTools()
        tools = toolsResult?.tools?.map {
            ToolUnion.ofTool(
                Tool.builder()
                    .name(it.name)
                    .description(it.description ?: "")
                    .inputSchema(
                        Tool.InputSchema.builder()
                            .type(JsonValue.from(it.inputSchema.type))
                            .properties(it.inputSchema.properties.toJsonValue())
                            .putAdditionalProperty("required", JsonValue.from(it.inputSchema.required))
                            .build()
                    )
                    .build()
            )
        } ?: emptyList()
        println("Connected to server with tools: ${tools.joinToString(", ") { it.tool().get().name() }}")
    } catch (e: Exception) {
        println("Failed to connect to MCP server: $e")
        throw e
    }
```

--------------------------------

### Main Execution Entry Point (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Sets up the main execution flow for the MCP client. It checks for command-line arguments (server script path), initializes the MCPClient, connects to the server, runs the chat loop, and ensures cleanup occurs even if errors happen.

```python
async def main():
        if len(sys.argv) < 2:
            print("Usage: python client.py <path_to_server_script>")
            sys.exit(1)

        client = MCPClient()
        try:
            await client.connect_to_server(sys.argv[1])
            await client.chat_loop()
        finally:
            await client.cleanup()

    if __name__ == "__main__":
        import sys
        asyncio.run(main())
```

--------------------------------

### Client Resuming SSE Stream with Last-Event-ID

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

Shows how a client can request to resume a broken SSE stream by including the Last-Event-ID header in a subsequent HTTP GET request.

```HTTP
GET /mcp-endpoint HTTP/1.1
Host: your-server.com
Accept: text/event-stream
Last-Event-ID: "unique-event-id-123"


```

--------------------------------

### Get Weather Alerts (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Fetches active weather alerts for a specified US state using the NWS API. It handles cases where no alerts are found or the API request fails, returning formatted alert strings.

```Python
async def get_alerts(state: str) -> str:
    """Get weather alerts for a US state.

    Args:
        state: Two-letter US state code (e.g. CA, NY)
    """
    url = f"{NWS_API_BASE}/alerts/active/area/{state}"
    data = await make_nws_request(url)

    if not data or "features" not in data:
        return "Unable to fetch alerts or no alerts found."

    if not data["features"]:
        return "No active alerts for this state."

    alerts = [format_alert(feature) for feature in data["features"]]
    return "\n---\n".join(alerts)
```

--------------------------------

### Basic MCP Client Connection Logic

Source: https://modelcontextprotocol.io/docs/develop/build-client

A Kotlin code snippet demonstrating the core logic for initializing and connecting the MCP client to a server, followed by initiating a chat session.

```kotlin
if (args.isEmpty()) throw IllegalArgumentException("Usage: java -jar <your_path>/build/libs/kotlin-mcp-client-0.1.0-all.jar <path_to_server_script>")
        val serverPath = args.first()
        val client = MCPClient()
        client.use {
            client.connectToServer(serverPath)
            client.chatLoop()
        }
```

--------------------------------

### Register Get Forecast Tool

Source: https://modelcontextprotocol.io/docs/develop/build-server

Registers a 'get_forecast' tool to retrieve weather forecasts based on latitude and longitude. It first fetches grid point data from the NWS API using the coordinates, then retrieves the forecast URL from the grid point data, and finally fetches and formats the forecast periods. Dependencies include z, makeNWSRequest, NWS_API_BASE, and the ForecastPeriod interface.

```typescript
server.tool(
  "get_forecast",
  "Get weather forecast for a location",
  {
    latitude: z.number().min(-90).max(90).describe("Latitude of the location"),
    longitude: z
      .number()
      .min(-180)
      .max(180)
      .describe("Longitude of the location"),
  },
  async ({ latitude, longitude }) => {
    // Get grid point data
    const pointsUrl = `${NWS_API_BASE}/points/${latitude.toFixed(4)},${longitude.toFixed(4)}`;
    const pointsData = await makeNWSRequest<PointsResponse>(pointsUrl);

    if (!pointsData) {
      return {
        content: [
          {
            type: "text",
            text: `Failed to retrieve grid point data for coordinates: ${latitude}, ${longitude}. This location may not be supported by the NWS API (only US locations are supported).`,
          },
        ],
      };
    }

    const forecastUrl = pointsData.properties?.forecast;
    if (!forecastUrl) {
      return {
        content: [
          {
            type: "text",
            text: "Failed to get forecast URL from grid point data",
          },
        ],
      };
    }

    // Get forecast data
    const forecastData = await makeNWSRequest<ForecastResponse>(forecastUrl);
    if (!forecastData) {
      return {
        content: [
          {
            type: "text",
            text: "Failed to retrieve forecast data",
          },
        ],
      };
    }

    const periods = forecastData.properties?.periods || [];
    if (periods.length === 0) {
      return {
        content: [
          {
            type: "text",
            text: "No forecast periods available",
          },
        ],
      };
    }

    // Format forecast periods
    const formattedForecast = periods.map((period: ForecastPeriod) =>
      [
        `${period.name || "Unknown"}:`,
        `Temperature: ${period.temperature || "Unknown"}°${period.temperatureUnit || "F"}`,
        `Wind: ${period.windSpeed || "Unknown"} ${period.windDirection || ""}`,
        `${period.shortForecast || "No forecast available"}`,
        "---",
      ].join("\n"),
    );

    const forecastText = `Forecast for ${latitude}, ${longitude}:\n\n${formattedForecast.join("\n")}`;

    return {
      content: [
        {
          type: "text",
          text: forecastText,
        },
      ],
    };
  },
);
```

--------------------------------

### Register Get Alerts Tool

Source: https://modelcontextprotocol.io/docs/develop/build-server

Registers a 'get_alerts' tool with the server to fetch weather alerts for a specified US state. It takes a two-letter state code as input, makes a request to the NWS API, formats the alerts using the formatAlert function, and returns the results as text content. Dependencies include z (for validation), makeNWSRequest, NWS_API_BASE, and the formatAlert function.

```typescript
// Register weather tools
server.tool(
  "get_alerts",
  "Get weather alerts for a state",
  {
    state: z.string().length(2).describe("Two-letter state code (e.g. CA, NY)"),
  },
  async ({ state }) => {
    const stateCode = state.toUpperCase();
    const alertsUrl = `${NWS_API_BASE}/alerts?area=${stateCode}`;
    const alertsData = await makeNWSRequest<AlertsResponse>(alertsUrl);

    if (!alertsData) {
      return {
        content: [
          {
            type: "text",
            text: "Failed to retrieve alerts data",
          },
        ],
      };
    }

    const features = alertsData.features || [];
    if (features.length === 0) {
      return {
        content: [
          {
            type: "text",
            text: `No active alerts for ${stateCode}`,
          },
        ],
      };
    }

    const formattedAlerts = features.map(formatAlert);
    const alertsText = `Active alerts for ${stateCode}:\n\n${formattedAlerts.join("\n")}`;

    return {
      content: [
        {
          type: "text",
          text: alertsText,
        },
      ],
    };
  },
);
```

--------------------------------

### Fetch Weather Forecast using HttpClient

Source: https://modelcontextprotocol.io/docs/develop/build-server

An extension function for HttpClient that retrieves weather forecast data. It first fetches point data based on latitude and longitude, then uses the forecast URL to get detailed period information, mapping it to a list of formatted strings.

```kotlin
// Extension function to fetch forecast information for given latitude and longitude
    suspend fun HttpClient.getForecast(latitude: Double, longitude: Double): List<String> {
        val points = this.get("/points/$latitude,$longitude").body<Points>()
        val forecast = this.get(points.properties.forecast).body<Forecast>()
        return forecast.properties.periods.map {
            """
                ${it.name}:
                Temperature: ${it.temperature} ${it.temperatureUnit}
                Wind: ${it.windSpeed} ${it.windDirection}
                Forecast: ${it.detailedForecast}
            """.trimIndent()
        }
    }
```

--------------------------------

### Tool Execution Error Example

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Shows a tool execution error reported within a JSON-RPC result, indicating a failure during tool operation (e.g., API rate limit exceeded) with 'isError' set to true.

```json
{
  "jsonrpc": "2.0",
  "id": 4,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Failed to fetch weather data: API rate limit exceeded"
      }
    ],
    "isError": true
  }
}
```

--------------------------------

### Request Completions for Prompt Argument (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/completion

A client sends a 'completion/complete' request to get suggestions for a specific argument within a prompt. The request includes a reference to the prompt and the argument's current name and value.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "completion/complete",
  "params": {
    "ref": {
      "type": "ref/prompt",
      "name": "code_review"
    },
    "argument": {
      "name": "language",
      "value": "py"
    }
  }
}
```

--------------------------------

### Get Specific Prompt (MCP)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Clients retrieve a specific prompt by its name using the 'prompts/get' method, optionally providing arguments to customize it. Argument auto-completion can be leveraged via the completion API.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "prompts/get",
  "params": {
    "name": "code_review",
    "arguments": {
      "code": "def hello():\n    print('world')"
    }
  }
}
```

--------------------------------

### Define ModelPreferences Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for server preferences in model selection. Includes optional priorities for cost, intelligence, and speed, and an array of ModelHint objects. These preferences guide clients in choosing appropriate LLMs based on server-defined criteria.

```typescript
interface ModelPreferences {
  costPriority?: number;
  hints?: ModelHint[];
  intelligencePriority?: number;
  speedPriority?: number;
}
```

--------------------------------

### Add MCP SDK Packages in C#

Source: https://modelcontextprotocol.io/docs/develop/build-server

Commands to add the necessary Model Context Protocol SDK and hosting NuGet packages to a C# project.

```bash
# Add the Model Context Protocol SDK NuGet package
dotnet add package ModelContextProtocol --prerelease
# Add the .NET Hosting NuGet package
dotnet add package Microsoft.Extensions.Hosting
```

--------------------------------

### ListPromptsRequest Params - Cursor

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Details the optional 'params' object within the ListPromptsRequest interface. The 'cursor' property is an opaque token used for pagination, allowing the server to return results starting after a specific point.

```typescript
params?: {
  cursor?: string;
}
```

--------------------------------

### JSON-RPC 2.0 Message Structure

Source: https://modelcontextprotocol.io/specification/2025-06-18/index

The Model Context Protocol utilizes JSON-RPC 2.0 for communication. This example illustrates the basic structure of a JSON-RPC request, which includes a method, parameters, and an identifier.

```JSON
{
  "jsonrpc": "2.0",
  "method": "some.mcp.method",
  "params": { ... },
  "id": 1
}
```

--------------------------------

### ListToolsRequest Cursor Parameter

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Details the optional `cursor` parameter within the `params` object of `ListToolsRequest`. This cursor is an opaque token used for pagination, allowing clients to request results starting after a specific point.

```typescript
cursor?: string;
```

--------------------------------

### AI Application Initialization Pseudo-code

Source: https://modelcontextprotocol.io/docs/learn/architecture

Pseudo-code illustrating how an AI application initializes MCP client sessions, registers servers based on capabilities, and sets server readiness.

```python
# Pseudo Code
async with stdio_client(server_config) as (read, write):
    async with ClientSession(read, write) as session:
        init_response = await session.initialize()
        if init_response.capabilities.tools:
            app.register_mcp_server(session, supports_tools=True)
        app.set_server_ready(session)
```

--------------------------------

### Connect Claude to Remote MCP Servers via Custom Connectors

Source: https://modelcontextprotocol.io/docs/develop/connect-remote-servers

This guide demonstrates connecting Claude to remote MCP servers using Custom Connectors. It covers connecting to existing third-party servers and building your own.

```Markdown
[Connect Claude to existing remote MCP servers](https://support.anthropic.com/en/articles/11175166-getting-started-with-custom-connectors-using-remote-mcp)
[Build your own remote MCP servers to connect with any tool](https://support.anthropic.com/en/articles/11503834-building-custom-connectors-via-remote-mcp-servers)
```

--------------------------------

### Running the MCP Client (Bash)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Demonstrates how to run the MCP client from the command line. It shows the syntax for specifying Python and Node.js server scripts using the `uv run` command.

```bash
uv run client.py path/to/server.py # python server
uv run client.py path/to/build/index.js # node server
```

--------------------------------

### Create MCP Client Manually with Java

Source: https://modelcontextprotocol.io/docs/develop/build-server

Demonstrates how to manually create and use an MCP client in Java to connect to an MCP server. It includes initializing the client, listing tools, calling tools, and closing the client.

```java
var stdioParams = ServerParameters.builder("java")
  .args("-jar", "/ABSOLUTE/PATH/TO/PARENT/FOLDER/mcp-weather-stdio-server-0.0.1-SNAPSHOT.jar")
  .build();

var stdioTransport = new StdioClientTransport(stdioParams);

var mcpClient = McpClient.sync(stdioTransport).build();

mcpClient.initialize();

ListToolsResult toolsList = mcpClient.listTools();

CallToolResult weather = mcpClient.callTool(
  new CallToolRequest("getWeatherForecastByLocation",
      Map.of("latitude", "47.6062", "longitude", "-122.3321")));

CallToolResult alert = mcpClient.callTool(
  new CallToolRequest("getAlerts", Map.of("state", "NY")));

mcpClient.closeGracefully();
```

--------------------------------

### Configure Claude for Desktop (JSON - Windows)

Source: https://modelcontextprotocol.io/docs/develop/build-server

A JSON configuration snippet for `claude_desktop_config.json` on Windows. It specifies how to launch the 'weather' MCP server using the 'uv' command, with Windows-style paths.

```JSON
{
  "mcpServers": {
    "weather": {
      "command": "uv",
      "args": [
        "--directory",
        "C:\\ABSOLUTE\\PATH\\TO\\PARENT\\FOLDER\\weather",
        "run",
        "weather.py"
      ]
    }
  }
}
```

--------------------------------

### Add Spring AI MCP Client Boot Starter Dependency

Source: https://modelcontextprotocol.io/docs/develop/build-server

Shows how to add the Spring AI MCP Client boot starter dependency to a Maven project for easy integration.

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-mcp-client</artifactId>
</dependency>
```

--------------------------------

### Initialize MCPClient and Anthropic Client

Source: https://modelcontextprotocol.io/docs/develop/build-client

Sets up the main client class with necessary imports, environment variable handling, and initializes the Anthropic and MCP clients.

```typescript
import { Anthropic } from "@anthropic-ai/sdk";
import {
  MessageParam,
  Tool,
} from "@anthropic-ai/sdk/resources/messages/messages.mjs";
import { Client } from "@modelcontextprotocol/sdk/client/index.js";
import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";
import readline from "readline/promises";
import dotenv from "dotenv";

dotenv.config();

const ANTHROPIC_API_KEY = process.env.ANTHROPIC_API_KEY;
if (!ANTHROPIC_API_KEY) {
  throw new Error("ANTHROPIC_API_API_KEY is not set");
}

class MCPClient {
  private mcp: Client;
  private anthropic: Anthropic;
  private transport: StdioClientTransport | null = null;
  private tools: Tool[] = [];

  constructor() {
    this.anthropic = new Anthropic({
      apiKey: ANTHROPIC_API_KEY,
    });
    this.mcp = new Client({ name: "mcp-client-cli", version: "1.0.0" });
  }
  // methods will go here
}
```

--------------------------------

### MCP Server Configuration for Brave Search

Source: https://modelcontextprotocol.io/docs/develop/build-client

Defines the configuration for the Brave Search MCP server within the `mcp-servers-config.json` file. It specifies the command to execute (npx), arguments for launching the Brave Search MCP server, and environment variables including the Brave Search API key.

```json
{
  "mcpServers": {
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": {
        "BRAVE_API_KEY": "<PUT YOUR BRAVE API KEY>"
      }
    }
  }
}
```

--------------------------------

### Launch Java Weather Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

Instructions on how to launch a Java-based MCP server for weather data. This involves running a JAR file with specific arguments.

```json
{
  "command": "run",
  "args": [
    "-jar",
    "C:\\PATH\\TO\\PARENT\\FOLDER\\weather\\build\\libs\\weather-0.1.0-all.jar"
  ]
}
```

--------------------------------

### Connect to MCP Server (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Implements the 'connect_to_server' method to establish a connection with an MCP server using its script path. It handles script type detection (.py or .js), configures server parameters, establishes a stdio transport, and initializes the client session. It also lists and prints the names of available tools on the server.

```python
async def connect_to_server(self, server_script_path: str):
    """Connect to an MCP server

    Args:
        server_script_path: Path to the server script (.py or .js)
    """
    is_python = server_script_path.endswith('.py')
    is_js = server_script_path.endswith('.js')
    if not (is_python or is_js):
        raise ValueError("Server script must be a .py or .js file")

    command = "python" if is_python else "node"
    server_params = StdioServerParameters(
        command=command,
        args=[server_script_path],
        env=None
    )

    stdio_transport = await self.exit_stack.enter_async_context(stdio_client(server_params))
    self.stdio, self.write = stdio_transport
    self.session = await self.exit_stack.enter_async_context(ClientSession(self.stdio, self.write))

    await self.session.initialize()

    # List available tools
    response = await self.session.list_tools()
    tools = response.tools
    print("\nConnected to server with tools:", [tool.name for tool in tools])
```

--------------------------------

### Open Configuration File (Bash)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Command to open the Claude for Desktop configuration file on macOS or Linux using VS Code.

```Bash
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

--------------------------------

### Open Configuration File (PowerShell)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Command to open the Claude for Desktop configuration file on Windows using VS Code.

```PowerShell
code $env:AppData\Claude\claude_desktop_config.json
```

--------------------------------

### Discover and Execute Server Tools

Source: https://modelcontextprotocol.io/docs/learn/architecture

Clients can discover available tools on an MCP server by using the 'tools/list' method. After discovery, clients can execute these tools to perform actions. This dynamic listing allows for flexible integration.

```JSON-RPC
{
  "method": "tools/list",
  "jsonrpc": "2.0",
  "id": 1
}

{
  "method": "tools/call",
  "params": {
    "tool_name": "example_tool",
    "arguments": { ... }
  },
  "jsonrpc": "2.0",
  "id": 2
}
```

--------------------------------

### Configure MCP Weather Server for Windows

Source: https://modelcontextprotocol.io/docs/develop/build-server

Configuration for launching an MCP weather server using Java on Windows. It specifies the command to run and arguments, including the JAR file path with Windows-style paths.

```json
{
  "mcpServers": {
    "spring-ai-mcp-weather": {
      "command": "java",
      "args": [
        "-Dspring.ai.mcp.server.transport=STDIO",
        "-jar",
        "C:\\ABSOLUTE\\PATH\\TO\\PARENT\\FOLDER\\weather\\mcp-weather-stdio-server-0.0.1-SNAPSHOT.jar"
      ]
    }
  }
}
```

--------------------------------

### Run Server Script with Client (Bash)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Executes a client script and specifies a server script to run, demonstrating relative and absolute path usage for server scripts.

```bash
# Relative path
uv run client.py ./server/weather.py

# Absolute path
uv run client.py /Users/username/projects/mcp-server/weather.py

# Windows path (either format works)
uv run client.py C:/projects/mcp-server/weather.py
uv run client.py C:\\projects\\mcp-server\\weather.py
```

--------------------------------

### Run MCP Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

This Kotlin code snippet shows the main function required to run the MCP server. It calls a specific function to initiate the server process.

```kotlin
fun main() = `run mcp server`()
```

--------------------------------

### Configure Filesystem Server (Windows)

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

This JSON configuration enables the Filesystem Server for Claude Desktop on Windows. It specifies the command to run ('npx') and arguments, including the server package and user-defined directories for file access, using Windows-style paths.

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "C:\\Users\\username\\Desktop",
        "C:\\Users\\username\\Downloads"
      ]
    }
  }
}
```

--------------------------------

### Configure Gradle Dependencies for Spring AI MCP Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

Sets up the necessary Spring AI starter and Spring Web dependencies in a Gradle build file. This is the first step in integrating Spring AI's Model Context Protocol server capabilities.

```groovy
dependencies {
  implementation platform("org.springframework.ai:spring-ai-starter-mcp-server")
  implementation platform("org.springframework:spring-web")
}
```

--------------------------------

### Configure Claude for Desktop (JSON - macOS/Linux)

Source: https://modelcontextprotocol.io/docs/develop/build-server

A JSON configuration snippet for `claude_desktop_config.json` on macOS and Linux. It specifies how to launch the 'weather' MCP server using the 'uv' command.

```JSON
{
  "mcpServers": {
    "weather": {
      "command": "uv",
      "args": [
        "--directory",
        "/ABSOLUTE/PATH/TO/PARENT/FOLDER/weather",
        "run",
        "weather.py"
      ]
    }
  }
}
```

--------------------------------

### JSON Configuration for MCP Servers in Claude Desktop

Source: https://modelcontextprotocol.io/docs/develop/build-server

Illustrates how to configure the `claude_desktop_config.json` file to include custom MCP servers. This JSON structure specifies the command and arguments needed to launch and run a server, such as the weather server.

```json
{
  "mcpServers": {
    "weather": {
      "command": "dotnet",
      "args": ["run", "--project", "/ABSOLUTE/PATH/TO/PROJECT", "--no-build"]
    }
  }
}
```

--------------------------------

### Gradle Plugins for Serialization and Shadow JAR

Source: https://modelcontextprotocol.io/docs/develop/build-server

Applies essential Gradle plugins: Kotlin serialization for handling JSON data and the Shadow plugin for creating fat JARs, which bundle all dependencies.

```kotlin
plugins {
    kotlin("plugin.serialization") version "your_version_of_kotlin"
    id("com.github.johnrengelman.shadow") version "8.1.1"
}
```

```groovy
plugins {
    id 'org.jetbrains.kotlin.plugin.serialization' version 'your_version_of_kotlin'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}
```

--------------------------------

### InitializeResult instructions Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Details the optional 'instructions' property of the InitializeResult interface. This string property provides guidance to the client on how to use the server's features, potentially aiding the LLM's understanding by being included in system prompts.

```typescript
  instructions?: string;
```

--------------------------------

### MCP Response for Listing Tools

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

The 'tools/list' response contains a list of available tools, each with a name, title, description, and input schema. It also includes 'nextCursor' for paginated results.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "tools": [
      {
        "name": "get_weather",
        "title": "Weather Information Provider",
        "description": "Get current weather information for a location",
        "inputSchema": {
          "type": "object",
          "properties": {
            "location": {
              "type": "string",
              "description": "City name or zip code"
            }
          },
          "required": ["location"]
        }
      }
    ],
    "nextCursor": "next-page-cursor"
  }
}
```

--------------------------------

### Run MCP Server with Stdio Transport

Source: https://modelcontextprotocol.io/docs/develop/build-server

Initializes and runs an MCP server using standard input/output for communication. It sets up a basic implementation named 'weather' and connects to the transport, waiting for the server to close.

```kotlin
// Main function to run the MCP server
fun `run mcp server`() {
    // Create the MCP Server instance with a basic implementation
    val server = Server(
        Implementation(
            name = "weather", // Tool name is "weather"
            version = "1.0.0" // Version of the implementation
        ),
        ServerOptions(
            capabilities = ServerCapabilities(tools = ServerCapabilities.Tools(listChanged = true))
        )
    )

    // Create a transport using standard IO for server communication
    val transport = StdioServerTransport(
        System.`in`.asInput(),
        System.out.asSink().buffered()
    )

    runBlocking {
        server.connect(transport)
        val done = Job()
        server.onClose {
            done.complete()
        }
        done.join()
    }
}
```

--------------------------------

### Define MCPClient Class (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Initializes the MCPClient class with necessary imports for asyncio, typing, contextlib, MCP client components, Anthropic, and dotenv. It sets up the client session, exit stack for resource management, and the Anthropic client instance, loading environment variables from a .env file.

```python
import asyncio
from typing import Optional
from contextlib import AsyncExitStack

from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client

from anthropic import Anthropic
from dotenv import load_dotenv

load_dotenv()  # load environment variables from .env

class MCPClient:
    def __init__(self):
        # Initialize session and client objects
        self.session: Optional[ClientSession] = None
        self.exit_stack = AsyncExitStack()
        self.anthropic = Anthropic()
    # methods will go here
```

--------------------------------

### Configure MCP Server in Claude Desktop (Windows)

Source: https://modelcontextprotocol.io/docs/develop/build-server

This JSON configuration snippet is added to the `claude_desktop_config.json` file to register a weather server on Windows. It specifies the command to run the server JAR and its arguments.

```json
{
  "mcpServers": {
    "weather": {
      "command": "java",
      "args": [

```

--------------------------------

### Invoke 'plan-vacation' Prompt with Arguments

Source: https://modelcontextprotocol.io/docs/learn/server-concepts

This JSON demonstrates how to invoke a prompt, specifically the 'plan-vacation' prompt, by providing the prompt name and a set of arguments. This structured input allows an AI to execute a defined workflow, such as planning a trip.

```json
{
  "prompt": "plan-vacation",
  "arguments": {
    "destination": "Barcelona",
    "departure_date": "2024-06-15",
    "return_date": "2024-06-22",
    "budget": 3000,
    "travelers": 2
  }
}
```

--------------------------------

### InitializeRequest Parameters

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Details the parameters required for the InitializeRequest. This includes client capabilities, client information, and the supported protocol version, which are crucial for establishing a compatible communication channel.

```typescript
params: {
  capabilities: ClientCapabilities;
  clientInfo: Implementation;
  protocolVersion: string;
}
```

--------------------------------

### Configure MCP Server in Claude Desktop (macOS/Linux)

Source: https://modelcontextprotocol.io/docs/develop/build-server

This JSON configuration snippet is added to the `claude_desktop_config.json` file to register a weather server. It specifies the command to run the server JAR and its arguments.

```json
{
  "mcpServers": {
    "weather": {
      "command": "java",
      "args": [
        "-jar",
        "/ABSOLUTE/PATH/TO/PARENT/FOLDER/weather/build/libs/weather-0.1.0-all.jar"
      ]
    }
  }
}
```

--------------------------------

### Run .NET Project for Claude for Desktop

Source: https://modelcontextprotocol.io/docs/develop/build-server

This snippet shows the command to execute a .NET project using the 'dotnet run' command. It includes arguments to specify the project path and disable building, which is relevant for launching MCP servers for applications like Claude for Desktop.

```bash
dotnet run --project C:\ABSOLUTE\PATH\TO\PROJECT --no-build
```

--------------------------------

### Initialize FastMCP Server and Define Constants (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Initializes the FastMCP server for a weather application and defines constants for the National Weather Service API base URL and user agent. The FastMCP class leverages Python type hints and docstrings for automatic tool definition.

```Python
from typing import Any
import httpx
from mcp.server.fastmcp import FastMCP

# Initialize FastMCP server
mcp = FastMCP("weather")

# Constants
NWS_API_BASE = "https://api.weather.gov"
USER_AGENT = "weather-app/1.0"
```

--------------------------------

### Sampling Flow Sequence Diagram

Source: https://modelcontextprotocol.io/docs/learn/client-concepts

Sequence diagram illustrating the sampling process, from server initiation to client-server response, including human-in-the-loop review by the user.

```mermaid
sequenceDiagram
    participant LLM
    participant User
    participant Client
    participant Server

    Note over Server,Client: Server initiates sampling
    Server->>Client: sampling/createMessage

    Note over Client,User: Human-in-the-loop review
    Client->>User: Present request for approval
    User-->>Client: Review and approve/modify

    Note over Client,LLM: Model interaction
    Client->>LLM: Forward approved request
    LLM-->>Client: Return generation

    Note over Client,User: Response review
    Client->>User: Present response for approval
    User-->>Client: Review and approve/modify

    Note over Server,Client: Complete request
    Client-->>Server: Return approved response
```

--------------------------------

### Define 'Plan a vacation' Prompt Structure

Source: https://modelcontextprotocol.io/docs/learn/server-concepts

This JSON defines the structure for a 'plan-vacation' prompt, including its name, title, description, and the arguments it accepts. Arguments have types, requirements, and optional descriptions, facilitating structured user input for AI workflows.

```json
{
  "name": "plan-vacation",
  "title": "Plan a vacation",
  "description": "Guide through vacation planning process",
  "arguments": [
    { "name": "destination", "type": "string", "required": true },
    { "name": "duration", "type": "number", "description": "days" },
    { "name": "budget", "type": "number", "required": false },
    { "name": "interests", "type": "array", "items": { "type": "string" } }
  ]
}
```

--------------------------------

### Process Query and Tool Execution (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Handles incoming content, identifies tool use, executes the specified tool with its arguments, and formats the tool's result to be sent back to the LLM. It manages the conversational context by appending assistant messages and tool results to the message history.

```python
assistant_message_content.append(content)
            elif content.type == 'tool_use':
                tool_name = content.name
                tool_args = content.input

                # Execute tool call
                result = await self.session.call_tool(tool_name, tool_args)
                final_text.append(f"[Calling tool {tool_name} with args {tool_args}]")

                assistant_message_content.append(content)
                messages.append({
                    "role": "assistant",
                    "content": assistant_message_content
                })
                messages.append({
                    "role": "user",
                    "content": [
                        {
                            "type": "tool_result",
                            "tool_use_id": content.id,
                            "content": result.content
                        }
                    ]
                })

                # Get next response from Claude
                response = self.anthropic.messages.create(
                    model="claude-3-5-sonnet-20241022",
                    max_tokens=1000,
                    messages=messages,
                    tools=available_tools
                )

                final_text.append(response.content[0].text)

        return "\n".join(final_text)
```

--------------------------------

### Spring AI MCP Client WebFlux Dependency

Source: https://modelcontextprotocol.io/docs/develop/build-client

This XML snippet shows how to add the Spring AI MCP client WebFlux starter to your project dependencies. This starter is recommended for production deployments using WebFlux and provides an SSE transport implementation.

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-mcp-client-webflux-spring-boot-starter</artifactId>
</dependency>
```

--------------------------------

### Model Context Protocol Initialization and Session Flow

Source: https://modelcontextprotocol.io/specification/2025-06-18/architecture/index

Illustrates the sequence of initialization and interaction between Host, Client, and Server in the Model Context Protocol, including capability negotiation, client requests, server requests, notifications, and session termination.

```mermaid
sequenceDiagram
    participant Host
    participant Client
    participant Server

    Host->>+Client: Initialize client
    Client->>+Server: Initialize session with capabilities
    Server-->>Client: Respond with supported capabilities

    Note over Host,Server: Active Session with Negotiated Features

    loop Client Requests
        Host->>Client: User- or model-initiated action
        Client->>Server: Request (tools/resources)
        Server-->>Client: Response
        Client-->>Host: Update UI or respond to model
    end

    loop Server Requests
        Server->>Client: Request (sampling)
        Client->>Host: Forward to AI
        Host-->>Client: AI response
        Client-->>Server: Response
    end

    loop Notifications
        Server--)Client: Resource updates
        Client--)Server: Status changes
    end

    Host->>Client: Terminate
    Client->>-Server: End session
    deactivate Server
```

--------------------------------

### Build Application with Maven

Source: https://modelcontextprotocol.io/docs/develop/build-client

Builds the Spring Boot application using Maven, compiling the code and packaging it for execution. This command also handles dependency management.

```bash
./mvnw clean install
```

--------------------------------

### package.json Configuration for MCP Weather Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

Shows the necessary additions to the `package.json` file for an MCP weather server project. It includes setting `"type": "module"`, defining a binary path, and adding a build script that compiles TypeScript and sets execute permissions.

```json
{
  "type": "module",
  "bin": {
    "weather": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js"
  },
  "files": ["build"]
}
```

--------------------------------

### Configure Filesystem Server (macOS)

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

This JSON configuration enables the Filesystem Server for Claude Desktop on macOS. It specifies the command to run ('npx') and arguments, including the server package and user-defined directories for file access.

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/username/Desktop",
        "/Users/username/Downloads"
      ]
    }
  }
}
```

--------------------------------

### Apply Shadow Plugin for Kotlin Gradle Build

Source: https://modelcontextprotocol.io/docs/develop/build-client

This code snippet demonstrates how to apply the `shadow` plugin in Gradle build scripts (`build.gradle.kts` and `build.gradle`). The shadow plugin is commonly used to create fat JARs, bundling all dependencies.

```kotlin
plugins {
    id("com.github.johnrengelman.shadow") version "8.1.1"
}
```

```groovy
plugins {
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}
```

--------------------------------

### Python Logging Best Practices

Source: https://modelcontextprotocol.io/docs/develop/build-server

Demonstrates correct and incorrect logging methods for STDIO-based MCP servers. Avoid writing to standard output (stdout) like `print()` in Python, and instead use logging libraries that write to stderr or files.

```Python
# ❌ Bad (STDIO)
print("Processing request")

# ✅ Good (STDIO)
import logging
logging.info("Processing request")
```

--------------------------------

### AI Application Tool Discovery Pseudo-code

Source: https://modelcontextprotocol.io/docs/learn/architecture

Illustrates how an AI application can fetch and aggregate tools from multiple MCP server sessions using Python-like pseudo-code.

```python
# Pseudo-code using MCP Python SDK patterns
available_tools = []
for session in app.mcp_server_sessions():
    tools_response = await session.list_tools()
    available_tools.extend(tools_response.tools)
conversation.register_available_tools(available_tools)
```

--------------------------------

### Configure MCP Weather Server for macOS/Linux

Source: https://modelcontextprotocol.io/docs/develop/build-server

Configuration for launching an MCP weather server using Java on macOS and Linux. It specifies the command to run and arguments, including the JAR file path.

```json
{
  "mcpServers": {
    "spring-ai-mcp-weather": {
      "command": "java",
      "args": [
        "-Dspring.ai.mcp.server.transport=STDIO",
        "-jar",
        "/ABSOLUTE/PATH/TO/PARENT/FOLDER/mcp-weather-stdio-server-0.0.1-SNAPSHOT.jar"
      ]
    }
  }
}
```

--------------------------------

### Spring AI ChatClient with MCP Tool Integration

Source: https://modelcontextprotocol.io/docs/develop/build-client

Demonstrates the Java code for building a Spring AI ChatClient. It configures the client with a default system prompt, integrates MCP tool callbacks using `mcpToolAdapter`, and sets up conversation memory using `InMemoryChatMemory`.

```java
var chatClient = chatClientBuilder
    .defaultSystem("You are useful assistant, expert in AI and Java.")
    .defaultToolCallbacks((Object[]) mcpToolAdapter.toolCallbacks())
    .defaultAdvisors(new MessageChatMemoryAdvisor(new InMemoryChatMemory()))
    .build();
```

--------------------------------

### Elicitation Flow Diagram

Source: https://modelcontextprotocol.io/docs/learn/client-concepts

Visualizes the sequence of interactions for elicitation, from server initiation to client returning user response.

```mermaid
sequenceDiagram
    participant User
    participant Client
    participant Server

    Note over Server,Client: Server initiates elicitation
    Server->>Client: elicitation/create

    Note over Client,User: Human interaction
    Client->>User: Present elicitation UI
    User-->>Client: Provide requested information

    Note over Server,Client: Complete request
    Client-->>Server: Return user response

    Note over Server: Continue processing with new information
```

--------------------------------

### Set Environment Variables for API Keys

Source: https://modelcontextprotocol.io/docs/develop/build-client

Sets the necessary environment variables for Anthropic API key and Brave Search API key. These keys are essential for the chatbot to authenticate with the respective services.

```bash
export ANTHROPIC_API_KEY='your-anthropic-api-key-here'
export BRAVE_API_KEY='your-brave-api-key-here'
```

--------------------------------

### Basic .NET MCP Client Initialization

Source: https://modelcontextprotocol.io/docs/develop/build-client

Sets up the basic structure of a .NET console application, configuring the host to read the API key from user secrets and initializing the MCP client transport.

```csharp
using Anthropic.SDK;
using Microsoft.Extensions.AI;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.Hosting;
using ModelContextProtocol.Client;
using ModelContextProtocol.Protocol.Transport;

var builder = Host.CreateApplicationBuilder(args);

builder.Configuration
    .AddEnvironmentVariables()
    .AddUserSecrets<Program>();

var (command, arguments) = GetCommandAndArguments(args);

var clientTransport = new StdioClientTransport(new() 
{
    Name = "Demo Server",
    Command = command,
    Arguments = arguments,
});

await using var mcpClient = await McpClientFactory.CreateAsync(clientTransport);

var tools = await mcpClient.ListToolsAsync();
foreach (var tool in tools)
{
    Console.WriteLine($"Connected to server with tools: {tool.Name}");
}
```

--------------------------------

### Process Query with Claude and Tools (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Defines the 'process_query' method to handle user queries using Claude and available tools. It formats messages, retrieves tool descriptions and schemas from the MCP session, makes an initial call to the Anthropic API, and prepares to process the response, including handling text and tool calls.

```python
async def process_query(self, query: str) -> str:
    """Process a query using Claude and available tools"""
    messages = [
        {
            "role": "user",
            "content": query
        }
    ]

    response = await self.session.list_tools()
    available_tools = [{
        "name": tool.name,
        "description": tool.description,
        "input_schema": tool.inputSchema
    } for tool in response.tools]

    # Initial Claude API call
    response = self.anthropic.messages.create(
        model="claude-3-5-sonnet-20241022",
        max_tokens=1000,
        messages=messages,
        tools=available_tools
    )

    # Process response and handle tool calls
    final_text = []

    assistant_message_content = []
    for content in response.content:
        if content.type == 'text':
            final_text.append(content.text)

```

--------------------------------

### MCP Initialization Request and Response

Source: https://modelcontextprotocol.io/docs/learn/architecture

Demonstrates the JSON-RPC 2.0 messages exchanged during the MCP client-server initialization phase, including capability negotiation and identity exchange.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "initialize",
  "params": {
    "protocolVersion": "2025-06-18",
    "capabilities": {
      "elicitation": {}
    },
    "clientInfo": {
      "name": "example-client",
      "version": "1.0.0"
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "protocolVersion": "2025-06-18",
    "capabilities": {
      "tools": {
        "listChanged": true
      },
      "resources": {}
    },
    "serverInfo": {
      "name": "example-server",
      "version": "1.0.0"
    }
  }
}
```

--------------------------------

### Run Filesystem Server Manually (macOS/Linux)

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

Manually execute the Filesystem Server using npx for macOS and Linux. This command helps diagnose connection issues by running the server directly from the command line.

```bash
npx -y @modelcontextprotocol/server-filesystem /Users/username/Desktop /Users/username/Downloads
```

--------------------------------

### Open Claude Desktop Config on macOS/Linux

Source: https://modelcontextprotocol.io/docs/develop/build-server

This bash command opens the Claude Desktop configuration file located at `~/Library/Application Support/Claude/claude_desktop_config.json` using VS Code.

```bash
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

--------------------------------

### Set Anthropic API Key

Source: https://modelcontextprotocol.io/docs/develop/build-client

Creates a .env file to securely store the Anthropic API key and adds it to .gitignore to prevent accidental commits.

```bash
echo "ANTHROPIC_API_KEY=<your key here>" > .env

echo ".env" >> .gitignore
```

--------------------------------

### MCP Tool Discovery Request and Response

Source: https://modelcontextprotocol.io/docs/learn/architecture

Demonstrates the JSON structure for requesting a list of available tools from an MCP server and the expected response format containing tool metadata.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/list"
}
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "tools": [
      {
        "name": "calculator_arithmetic",
        "title": "Calculator",
        "description": "Evaluate mathematical expressions",
        "inputSchema": {
          "type": "object",
          "properties": {
            "expression": {
              "type": "string",
              "description": "Mathematical expression to evaluate (e.g., '2 + 3 * 4', 'sin(30)', 'sqrt(16)')"
            }
          },
          "required": ["expression"]
        }
      },
      {
        "name": "weather_current",
        "title": "Weather Information",
        "description": "Get current weather information for any location worldwide",
        "inputSchema": {
          "type": "object",
          "properties": {
            "location": {
              "type": "string",
              "description": "City name, address, or coordinates (latitude,longitude)"
            },
            "units": {
              "type": "string",
              "enum": ["metric", "imperial", "kelvin"],
              "description": "Temperature units to use in response",
              "default": "metric"
            }
          },
          "required": ["location"]
        }
      }
    ]
  }
}
```

--------------------------------

### Connect to MCP Server with Script Path

Source: https://modelcontextprotocol.io/docs/develop/build-client

This Kotlin suspend function demonstrates how to establish a connection to an MCP server using a provided server script path. It constructs a command list based on the script's file extension (js, py, jar) and the operating system.

```kotlin
suspend fun connectToServer(serverScriptPath: String) {
    try {
        val command = buildList {
            when (serverScriptPath.substringAfterLast(".")) {
                "js" -> add("node")
                "py" -> add(if (System.getProperty("os.name").lowercase().contains("win")) "python" else "python3")
                "jar" -> addAll(listOf("java", "-jar"))

```

--------------------------------

### Add MCP Server Configuration (Windows)

Source: https://modelcontextprotocol.io/docs/develop/build-server

This JSON snippet shows how to add an MCP server configuration to the Claude for Desktop settings file on Windows. It specifies the command and arguments to run the 'weather' server.

```json
{
  "mcpServers": {
    "weather": {
      "command": "node",
      "args": ["C:\\PATH\\TO\\PARENT\\FOLDER\\weather\\build\\index.js"]
    }
  }
}
```

--------------------------------

### MCP Tool Execution Request and Response

Source: https://modelcontextprotocol.io/docs/learn/architecture

Shows the JSON format for calling a specific tool (`weather_current`) with arguments and the subsequent response containing the tool's output.

```json
{
  "jsonrpc": "2.0",
  "id": 3,
  "method": "tools/call",
  "params": {
    "name": "weather_current",
    "arguments": {
      "location": "San Francisco",
      "units": "imperial"
    }
  }
}
{
  "jsonrpc": "2.0",
  "id": 3,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Current weather in San Francisco: 68°F, partly cloudy with light winds from the west at 8 mph. Humidity: 65%"
      }
    ]
  }
}
```

--------------------------------

### Implement WeatherService with Spring AI @Tool Annotations

Source: https://modelcontextprotocol.io/docs/develop/build-server

Demonstrates how to create a Java service for fetching weather data using Spring's RestClient. It includes @Tool annotations to expose methods as MCP tools, specifying parameters and descriptions for interacting with the National Weather Service API.

```java
@Service
public class WeatherService {

	private final RestClient restClient;

	public WeatherService() {
		this.restClient = RestClient.builder()
			.baseUrl("https://api.weather.gov")
			.defaultHeader("Accept", "application/geo+json")
			.defaultHeader("User-Agent", "WeatherApiClient/1.0 (your@email.com)")
			.build();
	}

  @Tool(description = "Get weather forecast for a specific latitude/longitude")
  public String getWeatherForecastByLocation(
      double latitude,   // Latitude coordinate
      double longitude   // Longitude coordinate
  ) {
      // Returns detailed forecast including:
      // - Temperature and unit
      // - Wind speed and direction
      // - Detailed forecast description
  }

  @Tool(description = "Get weather alerts for a US state")
  public String getAlerts(
      @ToolParam(description = "Two-letter US state code (e.g. CA, NY)") String state
  ) {
      // Returns active alerts including:
      // - Event type
      // - Affected area
      // - Severity
      // - Description
      // - Safety instructions
  }

  // ......
}
```

--------------------------------

### Create Spring Boot MCP Server Application

Source: https://modelcontextprotocol.io/docs/develop/build-server

Sets up the main Spring Boot application class for the MCP server. It includes a @Bean definition to provide the WeatherService as a ToolCallbackProvider, enabling the MCP server to utilize the weather tools.

```java
@SpringBootApplication
public class McpServerApplication {

	public static void main(String[] args) {
		SpringApplication.run(McpServerApplication.class, args);
	}

	@Bean
	public ToolCallbackProvider weatherTools(WeatherService weatherService) {
		return  MethodToolCallbackProvider.builder().toolObjects(weatherService).build();
	}
}
```

--------------------------------

### Gradle Dependencies for MCP Kotlin SDK

Source: https://modelcontextprotocol.io/docs/develop/build-server

Configures project dependencies for the Model Context Protocol (MCP) Kotlin SDK, including Ktor client for content negotiation and JSON serialization, and SLF4J for logging.

```kotlin
implementation("io.modelcontextprotocol:kotlin-sdk:$mcpVersion")
implementation("org.slf4j:slf4j-nop:$slf4jVersion")
implementation("io.ktor:ktor-client-content-negotiation:$ktorVersion")
implementation("io.ktor:ktor-serialization-kotlinx-json:$ktorVersion")
```

```groovy
implementation "io.modelcontextprotocol:kotlin-sdk:$mcpVersion"
implementation "org.slf4j:slf4j-nop:$slf4jVersion"
implementation "io.ktor:ktor-client-content-negotiation:$ktorVersion"
implementation "io.ktor:ktor-serialization-kotlinx-json:$ktorVersion"
```

--------------------------------

### Basic MCP Client Kotlin Class

Source: https://modelcontextprotocol.io/docs/develop/build-client

This Kotlin code defines a basic `MCPClient` class that implements `AutoCloseable`. It initializes the Anthropic client and the MCP client, and includes a placeholder for methods and the implementation of the `close` method for resource management.

```kotlin
class MCPClient : AutoCloseable {
    private val anthropic = AnthropicOkHttpClient.fromEnv()
    private val mcp: Client = Client(clientInfo = Implementation(name = "mcp-client-cli", version = "1.0.0"))
    private lateinit var tools: List<ToolUnion>

    // methods will go here

    override fun close() {
        runBlocking {
            mcp.close()
            anthropic.close()
        }
    }
```

--------------------------------

### Configure Claude for Desktop (Windows)

Source: https://modelcontextprotocol.io/docs/develop/build-server

This PowerShell command opens the Claude for Desktop configuration file on Windows for editing. This file is used to configure MCP servers.

```powershell
code $env:AppData\Claude\claude_desktop_config.json
```

--------------------------------

### Connect to MCP Server

Source: https://modelcontextprotocol.io/docs/develop/build-client

Implements a method to establish a connection to an MCP server using a provided script path. It handles different script types (.js, .py) and dynamically sets the command based on the platform and script extension.

```typescript
async connectToServer(serverScriptPath: string) {
  try {
    const isJs = serverScriptPath.endsWith(".js");
    const isPy = serverScriptPath.endsWith(".py");
    if (!isJs && !isPy) {
      throw new Error("Server script must be a .js or .py file");
    }
    const command = isPy
      ? process.platform === "win32"
        ? "python"
        : "python3"
      : process.execPath;

    this.transport = new StdioClientTransport({
      command,
      args: [serverScriptPath],
    });
    await this.mcp.connect(this.transport);

    const toolsResult = await this.mcp.listTools();
    this.tools = toolsResult.tools.map((tool) => {
      return {
        name: tool.name,
        description: tool.description,
        input_schema: tool.inputSchema,
      };
    });
    console.log(
      "Connected to server with tools:",
      this.tools.map(({ name }) => name)
    );
  } catch (e) {
    console.log("Failed to connect to MCP server: ", e);
    throw e;
  }
}
```

--------------------------------

### Run Filesystem Server Manually (Windows)

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

Manually execute the Filesystem Server using npx for Windows. This command helps diagnose connection issues by running the server directly from the command line.

```powershell
npx -y @modelcontextprotocol/server-filesystem C:\Users\username\Desktop C:\Users\username\Downloads
```

--------------------------------

### Open Claude for Desktop Configuration File

Source: https://modelcontextprotocol.io/docs/develop/build-server

Commands to open the Claude for Desktop configuration file using VS Code on macOS/Linux and Windows. This file is used to configure MCP servers for integration with the desktop application.

```bash
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

```powershell
code $env:AppData\Claude\claude_desktop_config.json
```

--------------------------------

### Determine Server Command and Arguments

Source: https://modelcontextprotocol.io/docs/develop/build-client

This function parses command-line arguments to determine the correct command and arguments for launching the server based on the script type (.py, .js, or .csproj).

```csharp
static (string command, string[] arguments) GetCommandAndArguments(string[] args)
{
    return args switch
    {
        [var script] when script.EndsWith(".py") => ("python", args),
        [var script] when script.EndsWith(".js") => ("node", args),
        [var script] when Directory.Exists(script) || (File.Exists(script) && script.EndsWith(".csproj")) => ("dotnet", ["run", "--project", script, "--no-build"]),
        _ => throw new NotSupportedException("An unsupported server script was provided. Supported scripts are .py, .js, or .csproj")
    };
}
```

--------------------------------

### Tail MCP Logs (macOS/Linux)

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

View recent MCP logs and follow new entries in real-time for macOS and Linux. This is useful for diagnosing connection problems and understanding server behavior.

```bash
tail -n 20 -f ~/Library/Logs/Claude/mcp*.log
```

--------------------------------

### Implement Tool Execution Handler in Kotlin

Source: https://modelcontextprotocol.io/docs/develop/build-server

This Kotlin code sets up an HTTP client for API requests and registers two tools: 'get_alerts' to fetch weather alerts by state and 'get_forecast' to retrieve weather forecasts using latitude and longitude. It includes input validation and response mapping.

```kotlin
val httpClient = HttpClient {
    defaultRequest {
        url("https://api.weather.gov")
        headers {
            append("Accept", "application/geo+json")
            append("User-Agent", "WeatherApiClient/1.0")
        }
        contentType(ContentType.Application.Json)
    }
    install(ContentNegotiation) { json(Json { ignoreUnknownKeys = true }) }
}

server.addTool(
    name = "get_alerts",
    description = """
        Get weather alerts for a US state. Input is Two-letter US state code (e.g. CA, NY)
    """.trimIndent(),
    inputSchema = Tool.Input(
        properties = buildJsonObject {
            putJsonObject("state") {
                put("type", "string")
                put("description", "Two-letter US state code (e.g. CA, NY")
            }
        },
        required = listOf("state")
    )
) { request ->
    val state = request.arguments["state"]?.jsonPrimitive?.content
    if (state == null) {
        return@addTool CallToolResult(
            content = listOf(TextContent("The 'state' parameter is required."))
        )
    }

    val alerts = httpClient.getAlerts(state)

    CallToolResult(content = alerts.map { TextContent(it) })
}

server.addTool(
    name = "get_forecast",
    description = """
        Get weather forecast for a specific latitude/longitude
    """.trimIndent(),
    inputSchema = Tool.Input(
        properties = buildJsonObject {
            putJsonObject("latitude") { put("type", "number") }
            putJsonObject("longitude") { put("type", "number") }
        },
        required = listOf("latitude", "longitude")
    )
) { request ->
    val latitude = request.arguments["latitude"]?.jsonPrimitive?.doubleOrNull
    val longitude = request.arguments["longitude"]?.jsonPrimitive?.doubleOrNull
    if (latitude == null || longitude == null) {
        return@addTool CallToolResult(
            content = listOf(TextContent("The 'latitude' and 'longitude' parameters are required."))
        )
    }

    val forecast = httpClient.getForecast(latitude, longitude)

    CallToolResult(content = forecast.map { TextContent(it) })
}
```

--------------------------------

### Logging Best Practices in MCP Servers (JavaScript)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Demonstrates the correct and incorrect ways to log messages in MCP servers, emphasizing the use of stderr for STDIO-based servers to avoid corrupting JSON-RPC messages. `console.error` is recommended over `console.log` for STDIO servers.

```javascript
// ❌ Bad (STDIO)
console.log("Server started");

// ✅ Good (STDIO)
console.error("Server started"); // stderr is safe
```

--------------------------------

### Build MCP Server JAR using Maven

Source: https://modelcontextprotocol.io/docs/develop/build-server

Command to compile, test, and package the Spring AI MCP server application into an executable JAR file using Maven. The generated JAR will be located in the 'target' directory.

```bash
./mvnw clean install
```

--------------------------------

### Tool Definition with Output Schema

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Defines a tool named 'get_weather_data' with an input schema for location and an output schema for weather details like temperature, conditions, and humidity.

```json
{
  "name": "get_weather_data",
  "title": "Weather Data Retriever",
  "description": "Get current weather data for a location",
  "inputSchema": {
    "type": "object",
    "properties": {
      "location": {
        "type": "string",
        "description": "City name or zip code"
      }
    },
    "required": ["location"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "temperature": {
        "type": "number",
        "description": "Temperature in celsius"
      },
      "conditions": {
        "type": "string",
        "description": "Weather conditions description"
      },
      "humidity": {
        "type": "number",
        "description": "Humidity percentage"
      }
    },
    "required": ["temperature", "conditions", "humidity"]
  }
}
```

--------------------------------

### MCP Request to List Tools

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Clients use the 'tools/list' JSON RPC method to discover available tools. This request supports pagination via an optional 'cursor' parameter.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/list",
  "params": {
    "cursor": "optional-cursor-value"
  }
}
```

--------------------------------

### Client Information

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Provides information about the client implementation, including its name and version. This data is sent during initialization to identify the client to the server.

```typescript
clientInfo: Implementation;
```

--------------------------------

### Configure Spring AI MCP Client with Server Configuration

Source: https://modelcontextprotocol.io/docs/develop/build-server

Configures the Spring AI MCP client to use a server configuration file, pointing to the location of the client configuration.

```properties
spring.ai.mcp.client.stdio.servers-configuration=file:PATH/TO/claude_desktop_config.json
```

--------------------------------

### Tools List API

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/pagination

List available tools using pagination. Supports fetching tools in chunks using cursors.

```APIDOC
## POST /tools/list

### Description
List available tools. Supports pagination by cursor.

### Method
POST

### Endpoint
/tools/list

### Parameters
#### Request Body
- **jsonrpc** (string) - The JSON-RPC protocol version.
- **id** (string) - The request ID.
- **method** (string) - The method to call.
- **params** (object) - Parameters for the method.
  - **cursor** (string) - Optional - An opaque token for pagination. Omit for the first request.
```

--------------------------------

### Configure Claude for Desktop (macOS/Linux)

Source: https://modelcontextprotocol.io/docs/develop/build-server

This bash command opens the Claude for Desktop configuration file on macOS and Linux for editing. This file is used to configure MCP servers.

```bash
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

--------------------------------

### Java Spring AI MCP Server Dependencies

Source: https://modelcontextprotocol.io/docs/develop/build-server

This XML snippet lists the Maven dependencies required for a Spring AI MCP server project. It includes the starter for MCP servers and the Spring Web dependency.

```xml
<dependencies>
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-starter-mcp-server</artifactId>
    </dependency>

    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-web</artifactId>
    </dependency>
</dependencies>
```

--------------------------------

### Add MCP Server Configuration (macOS/Linux)

Source: https://modelcontextprotocol.io/docs/develop/build-server

This JSON snippet shows how to add an MCP server configuration to the Claude for Desktop settings file on macOS and Linux. It specifies the command and arguments to run the 'weather' server.

```json
{
  "mcpServers": {
    "weather": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/PARENT/FOLDER/weather/build/index.js"]
    }
  }
}
```

--------------------------------

### Configure Anthropic API Key using User Secrets

Source: https://modelcontextprotocol.io/docs/develop/build-client

This sequence initializes .NET user secrets and sets the Anthropic API key, ensuring secure management of sensitive credentials.

```bash
dotnet user-secrets init
dotnet user-secrets set "ANTHROPIC_API_KEY" "<your key here>"
```

--------------------------------

### Interactive Chat Loop (Kotlin)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Implements the main interactive chat loop. It prompts the user for input, processes the query using the `processQuery` function, and displays the response. The loop continues until the user types 'quit'.

```kotlin
suspend fun chatLoop() {
    println("\nMCP Client Started!")
    println("Type your queries or 'quit' to exit.")

    while (true) {
        print("\nQuery: ")
        val message = readLine() ?: break
        if (message.lowercase() == "quit") break
        val response = processQuery(message)
        println("\n$response")
    }
}
```

--------------------------------

### ModelPreferences: Hints (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Provides optional hints for model selection. Clients should evaluate these hints in the order they appear, taking the first match. Hints offer more granular control than numeric priorities and may be prioritized by the client.

```typescript
hints?: ModelHint[];
```

--------------------------------

### PowerShell Command to Open Claude Desktop Config

Source: https://modelcontextprotocol.io/docs/develop/build-server

Opens the Claude Desktop configuration file in VS Code on Windows systems. This allows users to edit the `claude_desktop_config.json` file to add MCP servers.

```powershell
code $env:AppData\Claude\claude_desktop_config.json
```

--------------------------------

### Add Kotlin SDK Dependencies

Source: https://modelcontextprotocol.io/docs/develop/build-client

This code block shows how to declare the necessary dependencies for the Kotlin MCP client project in `build.gradle.kts` (Kotlin DSL) and `build.gradle` (Groovy DSL). It includes versions for the MCP SDK, SLF4J, and the Anthropic Java client.

```kotlin
val mcpVersion = "0.4.0"
val slf4jVersion = "2.0.9"
val anthropicVersion = "0.8.0"

dependencies {
    implementation("io.modelcontextprotocol:kotlin-sdk:$mcpVersion")
    implementation("org.slf4j:slf4j-nop:$slf4jVersion")
    implementation("com.anthropic:anthropic-java:$anthropicVersion")
}
```

```groovy
def mcpVersion = '0.3.0'
def slf4jVersion = '2.0.9'
def anthropicVersion = '0.8.0'
dependencies {
    implementation "io.modelcontextprotocol:kotlin-sdk:$mcpVersion"
    implementation "org.slf4j:slf4j-nop:$slf4jVersion"
    implementation "com.anthropic:anthropic-java:$anthropicVersion"
}
```

--------------------------------

### Bash Command to Open Claude Desktop Config

Source: https://modelcontextprotocol.io/docs/develop/build-server

Opens the Claude Desktop configuration file in VS Code on macOS or Linux systems. This allows users to edit the `claude_desktop_config.json` file to add MCP servers.

```bash
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

--------------------------------

### MCP Architecture Diagram

Source: https://modelcontextprotocol.io/specification/2025-06-18/architecture/index

Visualizes the Model Context Protocol (MCP) architecture, showing the relationships between the Application Host Process, local machine resources (servers and resources), and internet-based resources.

```mermaid
graph LR
    subgraph "Application Host Process"
        H[Host]
        C1[Client 1]
        C2[Client 2]
        C3[Client 3]
        H --> C1
        H --> C2
        H --> C3
    end

    subgraph "Local machine"
        S1[Server 1<br>Files & Git]
        S2[Server 2<br>Database]
        R1[("Local<br>Resource A")]
        R2[("Local<br>Resource B")]

        C1 --> S1
        C2 --> S2
        S1 <--> R1
        S2 <--> R2
    end

    subgraph "Internet"
        S3[Server 3<br>External APIs]
        R3[("Remote<br>Resource C")]

        C3 --> S3
        S3 <--> R3
    end
```

--------------------------------

### List Resource Templates Response

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

A JSON RPC response containing a list of resource templates. Each template defines a URI template, name, title, description, and MIME type for accessing resources.

```json
{
  "jsonrpc": "2.0",
  "id": 3,
  "result": {
    "resourceTemplates": [
      {
        "uriTemplate": "file:///{path}",
        "name": "Project Files",
        "title": "📁 Project Files",
        "description": "Access files in the project directory",
        "mimeType": "application/octet-stream"
      }
    ]
  }
}
```

--------------------------------

### Open Claude Desktop Config on Windows

Source: https://modelcontextprotocol.io/docs/develop/build-server

This PowerShell command opens the Claude Desktop configuration file located at `$env:AppData\Claude\claude_desktop_config.json` using VS Code.

```powershell
code $env:AppData\Claude\claude_desktop_config.json
```

--------------------------------

### Run TypeScript MCP Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

This snippet shows how to run an MCP server using TypeScript. It connects the server to a StdioServerTransport and logs a message indicating the server is running.

```typescript
async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
  console.error("Weather MCP Server running on stdio");
}

main().catch((error) => {
  console.error("Fatal error in main():", error);
  process.exit(1);
});
```

--------------------------------

### InitializeRequest Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for an InitializeRequest sent from the client to the server. This request initiates the connection and includes essential information about the client's capabilities and the protocol version it supports.

```typescript
interface InitializeRequest {
  method: "initialize";
  params: {
    capabilities: ClientCapabilities;
    clientInfo: Implementation;
    protocolVersion: string;
  };
}
```

--------------------------------

### Define ResourceTemplate Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the ResourceTemplate interface, which describes resources available on the server. It includes optional fields like _meta, annotations, description, mimeType, and title, along with mandatory name and uriTemplate.

```TypeScript
interface ResourceTemplate {
  _meta?: { [key: string]: unknown };
  annotations?: Annotations;
  description?: string;
  mimeType?: string;
  name: string;
  title?: string;
  uriTemplate: string;
}
```

--------------------------------

### Reference Types for Completion (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/completion

The protocol supports two reference types for completions: 'ref/prompt' for prompts by name and 'ref/resource' for resource URIs.

```json
{
  "type": "ref/prompt",
  "name": "code_review"
}
```

```json
{
  "type": "ref/resource",
  "uri": "file:///{path}"
}
```

--------------------------------

### MCP Tools List Request

Source: https://modelcontextprotocol.io/docs/learn/architecture

A JSON-RPC 2.0 request sent by the client to discover the available tools on the MCP server, crucial for tool discovery.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "tools/list"
}
```

--------------------------------

### Configure package.json for TypeScript Modules

Source: https://modelcontextprotocol.io/docs/develop/build-client

Updates the package.json file to enable ES module support and defines a build script for compiling TypeScript and setting execute permissions.

```json
{
  "type": "module",
  "scripts": {
    "build": "tsc && chmod 755 build/index.js"
  }
}
```

--------------------------------

### Interactive Chat Loop and Cleanup (TypeScript)

Source: https://modelcontextprotocol.io/docs/develop/build-client

This code sets up an interactive command-line chat interface using Node.js 'readline'. It continuously prompts the user for input, processes queries via 'processQuery', and handles graceful shutdown by closing the readline interface and the MCP connection.

```typescript
    async chatLoop() {
      const rl = readline.createInterface({
        input: process.stdin,
        output: process.stdout,
      });

      try {
        console.log("\nMCP Client Started!");
        console.log("Type your queries or 'quit' to exit.");

        while (true) {
          const message = await rl.question("\nQuery: ");
          if (message.toLowerCase() === "quit") {
            break;
          }
          const response = await this.processQuery(message);
          console.log("\n" + response);
        }
      } finally {
        rl.close();
      }
    }

    async cleanup() {
      await this.mcp.close();
    }
```

--------------------------------

### Process Query with Anthropic and Handle Tool Calls (Kotlin)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Handles user queries by sending them to the Anthropic API, including available tools. It processes the response, extracting text content or handling tool use. When a tool is used, it calls the tool via the MCP, sends the tool result back to the LLM, and returns the final AI response. This function manages the conversation history by adding messages.

```kotlin
private val messageParamsBuilder: MessageCreateParams.Builder = MessageCreateParams.builder()
    .model(Model.CLAUDE_3_5_SONNET_20241022)
    .maxTokens(1024)

suspend fun processQuery(query: String): String {
    val messages = mutableListOf(
        MessageParam.builder()
            .role(MessageParam.Role.USER)
            .content(query)
            .build()
    )

    val response = anthropic.messages().create(
        messageParamsBuilder
            .messages(messages)
            .tools(tools)
            .build()
    )

    val finalText = mutableListOf<String>()
    response.content().forEach {
        when {
            it.isText() -> finalText.add(it.text().getOrNull()?.text() ?: "")

            it.isToolUse() -> {
                val toolName = it.toolUse().get().name()
                val toolArgs = 
                    it.toolUse().get()._input().convert(object : TypeReference<Map<String, JsonValue>>() {}) 

                val result = mcp.callTool(
                    name = toolName,
                    arguments = toolArgs ?: emptyMap()
                )
                finalText.add("[Calling tool $toolName with args $toolArgs]")

                messages.add(
                    MessageParam.builder()
                        .role(MessageParam.Role.USER)
                        .content(
                            """"type": "tool_result",
"tool_name": $toolName,
"result": ${result?.content?.joinToString("\n") { (it as TextContent).text ?: "" }}
"""".trimIndent()
                        )
                        .build()
                )

                val aiResponse = anthropic.messages().create(
                    messageParamsBuilder
                        .messages(messages)
                        .build()
                )

                finalText.add(aiResponse.content().first().text().getOrNull()?.text() ?: "")
            }
        }
    }

    return finalText.joinToString("\n", prefix = "", postfix = "")
}
```

--------------------------------

### MCP Prompts List Response

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

The response to a 'prompts/list' request includes a list of available prompts, each with a name, title, description, and defined arguments. It also includes a 'nextCursor' for pagination.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "prompts": [
      {
        "name": "code_review",
        "title": "Request Code Review",
        "description": "Asks the LLM to analyze code quality and suggest improvements",
        "arguments": [
          {
            "name": "code",
            "description": "The code to review",
            "required": true
          }
        ]
      }
    ],
    "nextCursor": "next-page-cursor"
  }
}
```

--------------------------------

### Define ListPromptsResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for the server's response to a prompt listing request. It includes optional metadata, an optional next cursor for pagination, and a required array of Prompt objects. It also allows for additional arbitrary string keys.

```typescript
interface ListPromptsResult {
  _meta?: { [key: string]: unknown };
  nextCursor?: string;
  prompts: Prompt[];
  [key: string]: unknown;
}
```

--------------------------------

### Spring AI MCP Client Configuration in application.yml

Source: https://modelcontextprotocol.io/docs/develop/build-client

Configures the Spring AI MCP client in the application's YAML properties file. This includes enabling the client, setting its name and version, specifying the type (SYNC), request timeout, and configuring stdio for server communication, as well as enabling tool callbacks.

```yaml
spring:
  ai:
    mcp:
      client:
        enabled: true
        name: brave-search-client
        version: 1.0.0
        type: SYNC
        request-timeout: 20s
        stdio:
          root-change-notification: true
          servers-configuration: classpath:/mcp-servers-config.json
        toolcallback:
          enabled: true
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
```

--------------------------------

### Define Root Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for a root directory or file that the server can operate on. It includes optional metadata, an optional name, and a mandatory URI.

```typescript
interface Root {
  _meta?: { [key: string]: unknown };
  name?: string;
  uri: string;
}
```

--------------------------------

### MCP Message Flow Diagram

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Sequence diagram illustrating the Model Context Protocol message flow, including tool discovery, selection, invocation, and updates between Client, Server, and LLM.

```mermaid
sequenceDiagram
    participant LLM
    participant Client
    participant Server

    Note over Client,Server: Discovery
    Client->>Server: tools/list
    Server-->>Client: List of tools

    Note over Client,LLM: Tool Selection
    LLM->>Client: Select tool to use

    Note over Client,Server: Invocation
    Client->>Server: tools/call
    Server-->>Client: Tool result
    Client->>LLM: Process result

    Note over Client,Server: Updates
    Server--)Client: tools/list_changed
    Client->>Server: tools/list
    Server-->>Client: Updated tools
```

--------------------------------

### Configure Application Properties for Spring AI MCP Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

Configures essential application properties for the Spring AI MCP server, including disabling the startup banner and clearing the console logging pattern. These settings help in streamlining the server's output.

```bash
spring.main.banner-mode=off
logging.pattern.console=
```

```yaml
logging:
  pattern:
    console: 
spring:
  main:
    banner-mode: off
```

--------------------------------

### List Resource Templates with Pagination (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the `ListResourceTemplatesRequest` interface for requesting a list of resource templates from the server. It includes an optional `params` object that can contain a `cursor` for pagination, allowing clients to fetch results in chunks.

```typescript
interface ListResourceTemplatesRequest {
  method: "resources/templates/list";
  params?: { cursor?: string };
}
```

--------------------------------

### Build C# MCP Weather Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

The core C# code to build an MCP server with standard I/O transport and register weather-related tools.

```csharp
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Hosting;
using ModelContextProtocol;
using System.Net.Http.Headers;

var builder = Host.CreateEmptyApplicationBuilder(settings: null);

builder.Services.AddMcpServer()
    .WithStdioServerTransport()
    .WithToolsFromAssembly();

builder.Services.AddSingleton(_ =>
{
    var client = new HttpClient() { BaseAddress = new Uri("https://api.weather.gov") };
    client.DefaultRequestHeaders.UserAgent.Add(new ProductInfoHeaderValue("weather-tool", "1.0"));
    return client;
});

var app = builder.Build();

await app.RunAsync();
```

--------------------------------

### MCP Client Dependencies for pom.xml

Source: https://modelcontextprotocol.io/docs/develop/build-client

Declares the required Maven dependencies for the Spring AI MCP client and the Anthropic model starter. These are crucial for enabling MCP client functionality and integrating with Anthropic AI.

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-mcp-client</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-model-anthropic</artifactId>
</dependency>
```

--------------------------------

### stdio Transport Sequence Diagram

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

Illustrates the communication flow for the stdio transport. The client launches the server as a subprocess, exchanging messages via stdin and stdout, with optional logging on stderr.

```mermaid
sequenceDiagram
    participant Client
    participant Server Process

    Client->>+Server Process: Launch subprocess
    loop Message Exchange
        Client->>Server Process: Write to stdin
        Server Process->>Client: Write to stdout
        Server Process--)Client: Optional logs on stderr
    end
    Client->>Server Process: Close stdin, terminate subprocess
    deactivate Server Process
```

--------------------------------

### Configure tsconfig.json for TypeScript Compilation

Source: https://modelcontextprotocol.io/docs/develop/build-client

Sets up the TypeScript compiler options, including target ECMAScript version, module resolution, output directory, and strict type checking.

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "outDir": "./build",
    "rootDir": "./",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["index.ts"],
  "exclude": ["node_modules"]
}
```

--------------------------------

### Request Completions with Context (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/completion

For prompts with multiple arguments, clients can provide previously resolved argument values in the 'context.arguments' object to help the server generate more relevant suggestions.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "completion/complete",
  "params": {
    "ref": {
      "type": "ref/prompt",
      "name": "code_review"
    },
    "argument": {
      "name": "framework",
      "value": "fla"
    },
    "context": {
      "arguments": {
        "language": "python"
      }
    }
  }
}
```

--------------------------------

### JSON Audio Content for LLM Messages

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Details the structure for audio content in LLM messages, mandating base64 encoding and a MIME type for audio-based context.

```json
{
  "type": "audio",
  "data": "base64-encoded-audio-data",
  "mimeType": "audio/wav"
}
```

--------------------------------

### GetPromptRequest Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for a request to retrieve a prompt from the server. It includes the method type and parameters such as prompt name and optional arguments for templating.

```typescript
interface GetPromptRequest {
  method: "prompts/get";
  params: {
    arguments?: {
      [key: string]: string;
    };
    name: string;
  };
}
```

--------------------------------

### Define Optional Tool Annotations

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Allows for optional additional tool information, such as a display name. The display name precedence order is title, annotations.title, then name.

```typescript
annotations?: ToolAnnotations
```

--------------------------------

### Fetch Active Weather Alerts using HttpClient

Source: https://modelcontextprotocol.io/docs/develop/build-server

An extension function for HttpClient that fetches active weather alerts for a specified state. It queries the alerts endpoint and maps the features to a list of strings, detailing the event, area, severity, description, and instructions.

```kotlin
// Extension function to fetch weather alerts for a given state
    suspend fun HttpClient.getAlerts(state: String): List<String> {
        val alerts = this.get("/alerts/active/area/$state").body<Alert>()
        return alerts.features.map {
            """
                Event: ${it.properties.event}
                Area: ${it.properties.areaDesc}
                Severity: ${it.properties.severity}
                Description: ${it.properties.description}
                Instruction: ${it.properties.instruction}
            """.trimIndent()
        }
    }
```

--------------------------------

### Resource Description Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the 'description' property for a resource, which provides a human-readable hint for LLMs about the resource's purpose. It is an optional string.

```typescript
description?: string
```

--------------------------------

### Mermaid Sequence Diagram for MCP Lifecycle

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle

Visualizes the sequence of interactions between a client and server during the MCP lifecycle, covering initialization, operation, and shutdown phases.

```mermaid
sequenceDiagram
    participant Client
    participant Server

    Note over Client,Server: Initialization Phase
    activate Client
    Client->>+Server: initialize request
    Server-->>Client: initialize response
    Client--)Server: initialized notification

    Note over Client,Server: Operation Phase
    rect rgb(200, 220, 250)
        note over Client,Server: Normal protocol operations
    end

    Note over Client,Server: Shutdown
    Client--)-Server: Disconnect
    deactivate Server
    Note over Client,Server: Connection closed
```

--------------------------------

### Interactive Chat Loop (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Implements the main interactive chat loop for the MCP client. It prompts the user for input, processes the query using the client's methods, prints the response, and handles potential exceptions. The loop continues until the user types 'quit'.

```python
async def chat_loop(self):
        """Run an interactive chat loop"""
        print("\nMCP Client Started!")
        print("Type your queries or 'quit' to exit.")

        while True:
            try:
                query = input("\nQuery: ").strip()

                if query.lower() == 'quit':
                    break

                response = await self.process_query(query)
                print("\n" + response)

            except Exception as e:
                print(f"\nError: {str(e)}")
```

--------------------------------

### Client Capabilities

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Represents the capabilities of the client, which are communicated to the server during the initialization phase. This allows the server to understand the client's features and limitations.

```typescript
capabilities: ClientCapabilities;
```

--------------------------------

### Process Query with Anthropic and MCP

Source: https://modelcontextprotocol.io/docs/develop/build-client

Handles user queries by sending them to the Anthropic API, processing the response, and executing any tool calls initiated by the model through the MCP client. It formats the output by concatenating text responses and logging tool calls.

```typescript
async processQuery(query: string) {
  const messages: MessageParam[] = [
    {
      role: "user",
      content: query,
    },
  ];

  const response = await this.anthropic.messages.create({
    model: "claude-3-5-sonnet-20241022",
    max_tokens: 1000,
    messages,
    tools: this.tools,
  });

  const finalText = [];

  for (const content of response.content) {
    if (content.type === "text") {
      finalText.push(content.text);
    } else if (content.type === "tool_use") {
      const toolName = content.name;
      const toolArgs = content.input as { [x: string]: unknown } | undefined;

      const result = await this.mcp.callTool({
        name: toolName,
        arguments: toolArgs,
      });
      finalText.push(
        `[Calling tool ${toolName} with args ${JSON.stringify(toolArgs)}]`
      );

      messages.push({
        role: "user",
        content: result.content as string,
      });
    }
  }
}
```

--------------------------------

### Resource Size Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the optional 'size' property for a resource, representing its raw content size in bytes. This is useful for displaying file sizes and estimating context window usage.

```typescript
size?: number
```

--------------------------------

### Define MCP Implementation Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the 'Implementation' interface for the Model Context Protocol (MCP). This interface includes properties for the implementation's name, an optional human-readable title, and its version. The 'name' is for programmatic use, while 'title' is for UI display.

```TypeScript
interface Implementation {
  name: string;
  title?: string;
  version: string;
}
```

--------------------------------

### Define MCP Tool Schema

Source: https://modelcontextprotocol.io/docs/learn/server-concepts

Defines a tool's structure, including its name, a description of its function, and a JSON Schema for validating its inputs. This allows AI models to understand and correctly invoke the tool.

```typescript
{
  name: "searchFlights",
  description: "Search for available flights",
  inputSchema: {
    type: "object",
    properties: {
      origin: { type: "string", description: "Departure city" },
      destination: { type: "string", description: "Arrival city" },
      date: { type: "string", format: "date", description: "Travel date" }
    },
    required: ["origin", "destination", "date"]
  }
}
```

--------------------------------

### List Available Prompts (MCP)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Clients request a list of available prompts using the 'prompts/list' method. This operation supports pagination via an optional 'cursor' parameter.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "prompts/list",
  "params": {
    "cursor": "optional-cursor-value"
  }
}
```

--------------------------------

### Define GetPromptResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the GetPromptResult interface, representing the server's response to a client's prompts/get request. It includes optional metadata, an optional description, and a mandatory array of PromptMessage objects.

```TypeScript
interface GetPromptResult {
  _meta?: { [key: string]: unknown };
  description?: string;
  messages: PromptMessage[];
  [key: string]: unknown;
}
```

--------------------------------

### Define Tool Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure of a 'Tool' interface, which represents a tool that a client can call. It includes optional metadata, annotations, description, and required input and output schemas.

```TypeScript
interface Tool {
  _meta?: { [key: string]: unknown };
  annotations?: ToolAnnotations;
  description?: string;
  inputSchema: {
    properties?: { [key: string]: object };
    required?: string[];
    type: "object";
  };
  name: string;
  outputSchema?: {
    properties?: { [key: string]: object };
    required?: string[];
    type: "object";
  };
  title?: string;
}
```

--------------------------------

### Resource Cleanup Function (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-client

Provides a method for cleaning up client resources. It ensures that asynchronous resources managed by `AsyncExitStack` are properly closed, preventing resource leaks.

```python
async def cleanup(self):
        """Clean up resources"""
        await self.exit_stack.aclose()
```

--------------------------------

### List Resource Templates Request

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

A JSON RPC request to list available resource templates exposed by the server. This is used for discovering parameterized resources.

```json
{
  "jsonrpc": "2.0",
  "id": 3,
  "method": "resources/templates/list"
}
```

--------------------------------

### Define ListToolsRequest Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the `ListToolsRequest` interface for requesting a list of tools from the server. It includes the `method` property set to 'tools/list' and an optional `params` object for pagination using a `cursor`.

```typescript
interface ListToolsRequest {
  method: "tools/list";
  params?: {
    cursor?: string;
  };
}
```

--------------------------------

### Resource Templates List API

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/pagination

List available resource templates using pagination. Supports fetching templates in chunks using cursors.

```APIDOC
## POST /resources/templates/list

### Description
List available resource templates. Supports pagination by cursor.

### Method
POST

### Endpoint
/resources/templates/list

### Parameters
#### Request Body
- **jsonrpc** (string) - The JSON-RPC protocol version.
- **id** (string) - The request ID.
- **method** (string) - The method to call.
- **params** (object) - Parameters for the method.
  - **cursor** (string) - Optional - An opaque token for pagination. Omit for the first request.
```

--------------------------------

### Define ServerCapabilities Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the ServerCapabilities interface, specifying optional properties for server features like completions, experimental capabilities, logging, prompt management, resource handling, and tool support. This interface allows for extensibility with custom server capabilities.

```typescript
interface ServerCapabilities {
  completions?: object;
  experimental?: { [key: string]: object };
  logging?: object;
  prompts?: { listChanged?: boolean };
  resources?: { listChanged?: boolean; subscribe?: boolean };
  tools?: { listChanged?: boolean };
}
```

--------------------------------

### ListResourceTemplatesResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for the server's response to a list resource templates request. It includes optional metadata, a cursor for pagination, and an array of ResourceTemplate objects.

```TypeScript
interface ListResourceTemplatesResult {
  _meta?: { [key: string]: unknown };
  nextCursor?: string;
  resourceTemplates: ResourceTemplate[];
  [key: string]: unknown;
}
```

--------------------------------

### Request Language Model Completion from Client

Source: https://modelcontextprotocol.io/docs/learn/architecture

MCP servers can request language model completions from a client's AI application using the 'sampling/complete' method. This allows servers to leverage client-side language models without needing to integrate a specific SDK.

```JSON-RPC
{
  "method": "sampling/complete",
  "params": {
    "prompt": "Your prompt here.",
    "max_tokens": 100
  },
  "jsonrpc": "2.0",
  "id": 3
}
```

--------------------------------

### Define ListToolsResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This interface defines the structure for the server's response when listing tools. It includes optional metadata, a cursor for pagination, and an array of Tool objects. It also allows for additional arbitrary string keys.

```TypeScript
interface ListToolsResult {
  _meta?: { [key: string]: unknown };
  nextCursor?: string;
  tools: Tool[];
  [key: string]: unknown;
}
```

--------------------------------

### Server Capabilities: Prompt Management

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Indicates if the server offers prompt templates and supports notifications for changes to the prompt list. This is an optional property.

```TypeScript
prompts?: {
  listChanged?: boolean
}
```

--------------------------------

### Declare MCP Resources Capability

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

This JSON snippet demonstrates how a server declares support for the 'resources' capability in the Model Context Protocol (MCP). It shows variations for supporting subscriptions, list change notifications, both, or neither.

```json
{
  "capabilities": {
    "resources": {
      "subscribe": true,
      "listChanged": true
    }
  }
}
```

```json
{
  "capabilities": {
    "resources": {}
  }
}
```

```json
{
  "capabilities": {
    "resources": {
      "subscribe": true
    }
  }
}
```

```json
{
  "capabilities": {
    "resources": {
      "listChanged": true
    }
  }
}
```

--------------------------------

### JSON Embedded Resource Content for LLM Messages

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Illustrates how to reference server-side resources within LLM messages, including URI, name, MIME type, and content for seamless integration.

```json
{
  "type": "resource",
  "resource": {
    "uri": "resource://example",
    "name": "example",
    "title": "My Example Resource",
    "mimeType": "text/plain",
    "text": "Resource content"
  }
}
```

--------------------------------

### Prompts List API

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/pagination

List available prompts using pagination. Supports fetching prompts in chunks using cursors.

```APIDOC
## POST /prompts/list

### Description
List available prompts. Supports pagination by cursor.

### Method
POST

### Endpoint
/prompts/list

### Parameters
#### Request Body
- **jsonrpc** (string) - The JSON-RPC protocol version.
- **id** (string) - The request ID.
- **method** (string) - The method to call.
- **params** (object) - Parameters for the method.
  - **cursor** (string) - Optional - An opaque token for pagination. Omit for the first request.
```

--------------------------------

### Troubleshoot Claude Desktop Logs

Source: https://modelcontextprotocol.io/docs/develop/build-server

This snippet shows how to check Claude's log files for Model Context Protocol (MCP) related errors. It specifically targets mcp.log for general MCP issues and mcp-server-SERVERNAME.log for server-specific errors. The command provided lists recent logs and follows new ones in real-time.

```bash
# Check Claude's logs for errors
tail -n 20 -f ~/Library/Logs/Claude/mcp*.log
```

--------------------------------

### Define InitializeResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure of the InitializeResult interface used in the Model Context Protocol. This interface represents the response from a server after an initialization request, containing server capabilities, protocol version, and optional metadata and instructions.

```typescript
interface InitializeResult {
  _meta?: { [key: string]: unknown };
  capabilities: ServerCapabilities;
  instructions?: string;
  protocolVersion: string;
  serverInfo: Implementation;
  [key: string]: unknown;
}
```

--------------------------------

### MCP Response for Listing Resources

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

This JSON snippet illustrates the response format from an MCP server when a client requests a list of resources. It includes an array of resource objects and a cursor for fetching the next page of results.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "resources": [
      {
        "uri": "file:///project/src/main.rs",
        "name": "main.rs",
        "title": "Rust Software Application Main File",
        "description": "Primary application entry point",
        "mimeType": "text/x-rust"
      }
    ],
    "nextCursor": "next-page-cursor"
  }
}
```

--------------------------------

### C# Weather Tools for National Weather Service API

Source: https://modelcontextprotocol.io/docs/develop/build-server

Defines C# static classes with MCP attributes to handle weather-related queries. Includes methods for fetching active alerts for a US state and retrieving weather forecasts for a given latitude and longitude. These methods interact with the National Weather Service API.

```csharp
using ModelContextProtocol.Server;
using System.ComponentModel;
using System.Globalization;
using System.Text.Json;

namespace QuickstartWeatherServer.Tools;

[McpServerToolType]
public static class WeatherTools
{
    [McpServerTool, Description("Get weather alerts for a US state.")]
    public static async Task<string> GetAlerts(
        HttpClient client,
        [Description("The US state to get alerts for.")] string state)
    {
        using var jsonDocument = await client.ReadJsonDocumentAsync($"/alerts/active/area/{state}");
        var jsonElement = jsonDocument.RootElement;
        var alerts = jsonElement.GetProperty("features").EnumerateArray();

        if (!alerts.Any())
        {
            return "No active alerts for this state.";
        }

        return string.Join("\n--\n", alerts.Select(alert =>
        {
            JsonElement properties = alert.GetProperty("properties");
            return $"""
                    Event: {properties.GetProperty("event").GetString()}
                    Area: {properties.GetProperty("areaDesc").GetString()}
                    Severity: {properties.GetProperty("severity").GetString()}
                    Description: {properties.GetProperty("description").GetString()}
                    Instruction: {properties.GetProperty("instruction").GetString()}
                    """;
        }));
    }

    [McpServerTool, Description("Get weather forecast for a location.")]
    public static async Task<string> GetForecast(
        HttpClient client,
        [Description("Latitude of the location.")] double latitude,
        [Description("Longitude of the location.")] double longitude)
    {
        var pointUrl = string.Create(CultureInfo.InvariantCulture, $"/points/{latitude},{longitude}");
        using var jsonDocument = await client.ReadJsonDocumentAsync(pointUrl);
        var forecastUrl = jsonDocument.RootElement.GetProperty("properties").GetProperty("forecast").GetString()
            ?? throw new Exception($"No forecast URL provided by {client.BaseAddress}points/{latitude},{longitude}");

        using var forecastDocument = await client.ReadJsonDocumentAsync(forecastUrl);
        var periods = forecastDocument.RootElement.GetProperty("properties").GetProperty("periods").EnumerateArray();

        return string.Join("\n---\n", periods.Select(period => $"""
                {period.GetProperty("name").GetString()}
                Temperature: {period.GetProperty("temperature").GetInt32()}°F
                Wind: {period.GetProperty("windSpeed").GetString()} {period.GetProperty("windDirection").GetString()}
                Forecast: {period.GetProperty("detailedForecast").GetString()}
                """));
    }
}

```

--------------------------------

### Progress Tracking Sequence Diagram (Mermaid)

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/utilities/progress

A Mermaid sequence diagram illustrating the flow of progress tracking, from the initial request with a `progressToken` to subsequent progress notifications and the final method response.

```mermaid
sequenceDiagram
    participant Sender
    participant Receiver

    Note over Sender,Receiver: Request with progress token
    Sender->>Receiver: Method request with progressToken

    Note over Sender,Receiver: Progress updates
    Receiver-->>Sender: Progress notification (0.2/1.0)
    Receiver-->>Sender: Progress notification (0.6/1.0)
    Receiver-->>Sender: Progress notification (1.0/1.0)

    Note over Sender,Receiver: Operation complete
    Receiver->>Sender: Method response
```

--------------------------------

### View MCP Logs (Windows)

Source: https://modelcontextprotocol.io/docs/develop/connect-local-servers

Display recent MCP logs for Windows. This command helps in diagnosing connection issues by showing the latest log entries.

```powershell
type "%APPDATA%\Claude\logs\mcp*.log"
```

--------------------------------

### MCP Request to Call a Tool

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Clients invoke tools using the 'tools/call' JSON RPC method, specifying the tool's name and its arguments.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "tools/call",
  "params": {
    "name": "get_weather",
    "arguments": {
      "location": "New York"
    }
  }
}
```

--------------------------------

### Define BlobResourceContents Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the BlobResourceContents interface, which represents the contents of a specific resource or sub-resource. It includes optional metadata, a base64-encoded blob, an optional MIME type, and a required URI.

```typescript
interface BlobResourceContents {
  _meta?: { [key: string]: unknown };
  blob: string;
  mimeType?: string;
  uri: string;
}
```

--------------------------------

### Resources List API

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/pagination

List available resources using pagination. Supports fetching resources in chunks using cursors.

```APIDOC
## POST /resources/list

### Description
List available resources. Supports pagination by cursor.

### Method
POST

### Endpoint
/resources/list

### Parameters
#### Request Body
- **jsonrpc** (string) - The JSON-RPC protocol version.
- **id** (string) - The request ID.
- **method** (string) - The method to call.
- **params** (object) - Parameters for the method.
  - **cursor** (string) - Optional - An opaque token for pagination. Omit for the first request.
```

--------------------------------

### Define Prompt Interface in TypeScript

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the TypeScript interface for a Prompt, including optional metadata, arguments, description, and a required name. The _meta property allows for arbitrary string keys mapping to unknown values, while arguments is an array of PromptArgument. The name is a string, and title is an optional string.

```TypeScript
interface Prompt {
  _meta?: { [key: string]: unknown };
  arguments?: PromptArgument[];
  description?: string;
  name: string;
  title?: string;
}
```

--------------------------------

### Request with Progress Token (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/utilities/progress

This JSON structure demonstrates how to include a `progressToken` in a request's metadata to enable progress tracking for the operation.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "some_method",
  "params": {
    "_meta": {
      "progressToken": "abc123"
    }
  }
}
```

--------------------------------

### Send Ping Request (JSON-RPC)

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/utilities/ping

This snippet shows the JSON-RPC request format for initiating a ping. It's a standard request with the method set to 'ping' and no parameters.

```json
{
  "jsonrpc": "2.0",
  "id": "123",
  "method": "ping"
}
```

--------------------------------

### Mermaid Sequence Diagram for LLM Message Flow

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Visualizes the message exchange between a Client and Server for prompt discovery and usage, including handling prompt list changes.

```mermaid
sequenceDiagram
    participant Client
    participant Server

    Note over Client,Server: Discovery
    Client->>Server: prompts/list
    Server-->>Client: List of prompts

    Note over Client,Server: Usage
    Client->>Server: prompts/get
    Server-->>Client: Prompt content

    opt listChanged
      Note over Client,Server: Changes
      Server--)Client: prompts/list_changed
      Client->>Server: prompts/list
      Server-->>Client: Updated prompts
    end
```

--------------------------------

### Respond with Roots List

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/roots

Clients respond to a 'roots/list' request with a list of roots, each defined by a URI and an optional name. The URI must be a 'file://' URI.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "roots": [
      {
        "uri": "file:///home/user/projects/myproject",
        "name": "My Project"
      }
    ]
  }
}
```

--------------------------------

### Secure Token Storage and OAuth Best Practices

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

Clients and servers must implement secure token storage and adhere to OAuth best practices to prevent token theft and unauthorized resource access.

```General
Clients and servers MUST implement secure token storage and follow OAuth best practices.
```

--------------------------------

### Define ListResourcesRequest Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the ListResourcesRequest interface, which specifies the method 'resources/list' and an optional 'params' object containing a 'cursor' for pagination. This interface is used to request a list of resources from the server.

```typescript
interface ListResourcesRequest {
  method: "resources/list";
  params?: { 
    cursor?: string 
  };
}
```

--------------------------------

### Request User Elicitation from Client

Source: https://modelcontextprotocol.io/docs/learn/architecture

Servers can request additional information or user confirmation from clients using the 'elicitation/request' method. This is useful for gathering necessary details or ensuring user consent before performing actions.

```JSON-RPC
{
  "method": "elicitation/request",
  "params": {
    "question": "Are you sure you want to proceed?"
  },
  "jsonrpc": "2.0",
  "id": 4
}
```

--------------------------------

### Add MCP Kotlin SDK Dependencies

Source: https://modelcontextprotocol.io/docs/develop/build-server

Specifies the dependencies required for the MCP Kotlin SDK in a Gradle build file.

```kotlin
val mcpVersion = "0.4.0"
val slf4jVersion = "2.0.9"
val ktorVersion = "3.1.1"

dependencies {
    implementation("io.modelcontextprotocol:kotlin-sdk:$mcpVersion")

```

--------------------------------

### Handle Tools Changed Notification in Application

Source: https://modelcontextprotocol.io/docs/learn/architecture

Handles real-time notifications about tool list changes within an AI application. It fetches the updated tool list, updates the application's available tools, and notifies the LLM of new capabilities.

```python
async def handle_tools_changed_notification(session):
    tools_response = await session.list_tools()
    app.update_available_tools(session, tools_response.tools)
    if app.conversation.is_active():
        app.conversation.notify_llm_of_new_capabilities()
```

--------------------------------

### Define Tool Input Schema

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the input schema for a tool using JSON Schema. It specifies properties, required fields, and the type as 'object'. This helps LLMs understand the expected parameters for a tool.

```typescript
inputSchema: {
  properties?: { [key: string]: string };
  required?: string[];
  type: "object";
}
```

--------------------------------

### InitializeResult protocolVersion Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Specifies the 'protocolVersion' property within the InitializeResult interface. This required string property indicates the version of the Model Context Protocol the server is using.

```typescript
  protocolVersion: string;
```

--------------------------------

### Define ResourceTemplate with MIME Type

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines a ResourceTemplate with an optional MIME type for resources matching the template. This is useful when all resources share the same MIME type.

```typescript
interface ResourceTemplate {
  mimeType?: string;
  name: string;
  title?: string;
  uriTemplate: string;
}
```

--------------------------------

### MCP Request to List Resources

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

This JSON snippet shows the structure of a JSON-RPC request sent by a client to list available resources via the Model Context Protocol (MCP). It includes an optional cursor for pagination.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "resources/list",
  "params": {
    "cursor": "optional-cursor-value"
  }
}
```

--------------------------------

### Server Capabilities: Tool Management

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Indicates if the server offers tools to call and supports notifications for changes to the tool list. This is an optional property.

```TypeScript
tools?: {
  listChanged?: boolean
}
```

--------------------------------

### Define CreateMessageResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure of the CreateMessageResult interface, used for client responses to message creation requests. It includes content, model, role, and optional metadata and stop reason.

```typescript
interface CreateMessageResult {
  _meta?: { [key: string]: unknown };
  content: TextContent | ImageContent | AudioContent;
  model: string;
  role: Role;
  stopReason?: string;
  [key: string]: unknown;
}
```

--------------------------------

### Define ListPromptsRequest Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the ListPromptsRequest interface, which specifies the structure for client requests to list prompts. It includes a mandatory 'method' property set to 'prompts/list' and an optional 'params' object for pagination.

```typescript
interface ListPromptsRequest {
  method: "prompts/list";
  params?: { cursor?: string };
}
```

--------------------------------

### MCP Client Query Processing Logic

Source: https://modelcontextprotocol.io/docs/develop/build-client

Implements the core chat functionality using the Anthropic client, configuring it for function invocation and processing user queries with streaming responses.

```csharp
using var anthropicClient = new AnthropicClient(new APIAuthentication(builder.Configuration["ANTHROPIC_API_KEY"]))
    .Messages
    .AsBuilder()
    .UseFunctionInvocation()
    .Build();

var options = new ChatOptions
{
    MaxOutputTokens = 1000,
    ModelId = "claude-3-5-sonnet-20241022",
    Tools = [.. tools]
};

Console.ForegroundColor = ConsoleColor.Green;
Console.WriteLine("MCP Client Started!");
Console.ResetColor();

PromptForInput();
while(Console.ReadLine() is string query && !"exit".Equals(query, StringComparison.OrdinalIgnoreCase))
{
    if (string.IsNullOrWhiteSpace(query))
    {
        PromptForInput();
        continue;
    }

    await foreach (var message in anthropicClient.GetStreamingResponseAsync(query, options))
    {
        Console.Write(message);
    }
    Console.WriteLine();

    PromptForInput();
}

static void PromptForInput()
{
    Console.WriteLine("Enter a command (or 'exit' to quit):");
    Console.ForegroundColor = ConsoleColor.Cyan;
    Console.Write("> ");
    Console.ResetColor();
}
```

--------------------------------

### Execute AI Tool Call in Application

Source: https://modelcontextprotocol.io/docs/learn/architecture

Handles tool calls within an AI application by finding the appropriate MCP session, executing the tool, and adding the result to the conversation context. This enables AI applications to dynamically invoke server functionality and integrate results into language model interactions.

```python
async def handle_tool_call(conversation, tool_name, arguments):
    session = app.find_mcp_session_for_tool(tool_name)
    result = await session.call_tool(tool_name, arguments)
    conversation.add_tool_result(result.content)
```

--------------------------------

### Completion Response with Suggestions (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/completion

The server responds with a 'completion/complete' result containing an array of suggested values, the total number of matches, and a flag indicating if more results are available.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "completion": {
      "values": ["python", "pytorch", "pyside"],
      "total": 10,
      "hasMore": true
    }
  }
}
```

--------------------------------

### Define AudioContent Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the AudioContent interface for representing audio data. It includes base64-encoded audio data, MIME type, and optional metadata and annotations.

```typescript
interface AudioContent {
  _meta?: { [key: string]: unknown };
  annotations?: Annotations;
  data: string;
  mimeType: string;
  type: "audio";
}
```

--------------------------------

### Define ListRootsRequest Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the ListRootsRequest interface, which specifies the structure for a request from a server to a client to list root URIs. It includes a fixed 'method' property and an optional 'params' object that can contain metadata like a progress token.

```typescript
interface ListRootsRequest {
  method: "roots/list";
  params?: {
    _meta?: {
      progressToken?: ProgressToken;
      [key: string]: unknown;
    };
    [key: string]: unknown;
  };
}
```

--------------------------------

### Pagination Request Format

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/pagination

Illustrates how to make a paginated request by including the 'cursor' parameter to fetch subsequent pages of results.

```APIDOC
## Request

### Method
POST

### Endpoint
/api/v1/some_resource

### Parameters
#### Request Body
- **jsonrpc** (string) - The JSON-RPC protocol version.
- **id** (string) - The request ID.
- **method** (string) - The method to call (e.g., `resources/list`).
- **params** (object) - Parameters for the method.
  - **cursor** (string) - Required/Optional - An opaque token representing the position in the result set, used to fetch subsequent pages. Omitted for the first request.
```

--------------------------------

### MCP Normal OAuth Proxy Usage Sequence Diagram

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/security_best_practices

Illustrates the standard OAuth authorization flow when an MCP proxy server interacts with a third-party authorization server, preserving user consent. This flow involves user-agent redirection, authorization requests, and token exchanges.

```mermaid
sequenceDiagram
    participant UA as User-Agent (Browser)
    participant MC as MCP Client
    participant M as MCP Proxy Server
    participant TAS as Third-Party Authorization Server

    Note over UA,M: Initial Auth flow completed

    Note over UA,TAS: Step 1: Legitimate user consent for Third Party Server

    M->>UA: Redirect to third party authorization server
    UA->>TAS: Authorization request (client_id: mcp-proxy)
    TAS->>UA: Authorization consent screen
    Note over UA: Review consent screen
    UA->>TAS: Approve
    TAS->>UA: Set consent cookie for client ID: mcp-proxy
    TAS->>UA: 3P Authorization code + redirect to mcp-proxy-server.com
    UA->>M: 3P Authorization code
    Note over M,TAS: Exchange 3P code for 3P token
    Note over M: Generate MCP authorization code
    M->>UA: Redirect to MCP Client with MCP authorization code

    Note over M,UA: Exchange code for token, etc.
```

--------------------------------

### Define ResourceContents Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the ResourceContents interface, which specifies the structure for resource content. It includes optional metadata, an optional MIME type, and a mandatory URI.

```TypeScript
interface ResourceContents {
  _meta?: { [key: string]: unknown };
  mimeType?: string;
  uri: string;
}
```

--------------------------------

### Declare MCP Tools Capability

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Servers must declare the 'tools' capability to indicate support for exposing tools to language models. The 'listChanged' flag specifies if notifications will be sent when the tool list is updated.

```json
{
  "capabilities": {
    "tools": {
      "listChanged": true
    }
  }
}
```

--------------------------------

### Define Tool Description

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Provides a human-readable description of the tool, acting as a hint for LLMs to improve their understanding of available tools. It can influence display name precedence.

```typescript
description?: string
```

--------------------------------

### Resource Parameter Implementation (RFC 8707)

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP clients MUST implement Resource Indicators for OAuth 2.0 (RFC 8707) by including the 'resource' parameter in authorization and token requests. This parameter explicitly specifies the target resource, which must be the canonical URI of the MCP server.

```text
MCP clients MUST implement Resource Indicators for OAuth 2.0 as defined in [RFC 8707](https://www.rfc-editor.org/rfc/rfc8707.html) to explicitly specify the target resource for which the token is being requested. The `resource` parameter:
1. MUST be included in both authorization requests and token requests.
2. MUST identify the MCP server that the client intends to use the token with.
3. MUST use the canonical URI of the MCP server as defined in [RFC 8707 Section 2](https://www.rfc-editor.org/rfc/rfc8707.html#name-access-token-request).
```

--------------------------------

### Session Hijack Prompt Injection Sequence Diagram

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/security_best_practices

Illustrates a session hijacking attack where an attacker exploits a shared session ID to inject malicious payloads through an MCP server and queue system, ultimately affecting the client.

```mermaid
sequenceDiagram
    participant Client
    participant ServerA
    participant Queue
    participant ServerB
    participant Attacker

    Client->>ServerA: Initialize (connect to streamable HTTP server)
    ServerA-->>Client: Respond with session ID

    Attacker->>ServerB: Access/guess session ID
    Note right of Attacker: Attacker knows/guesses session ID

    Attacker->>ServerB: Trigger event (malicious payload, using session ID)
    ServerB->>Queue: Enqueue event (keyed by session ID)

    ServerA->>Queue: Poll for events (using session ID)
    Queue-->>ServerA: Event data (malicious payload)

    ServerA-->>Client: Async response (malicious payload)
    Client->>Client: Acts based on malicious payload

```

--------------------------------

### Define Tool Output Schema

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the optional output schema for a tool using JSON Schema. It specifies properties and their types, allowing LLMs to understand the structure of the tool's output.

```typescript
outputSchema?: {
  properties?: { [key: string]: string };
}
```

--------------------------------

### Create Sampling Message Request

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/sampling

Servers send a 'sampling/createMessage' request to initiate a language model generation. This includes messages, model preferences, system prompt, and max tokens.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "sampling/createMessage",
  "params": {
    "messages": [
      {
        "role": "user",
        "content": {
          "type": "text",
          "text": "What is the capital of France?"
        }
      }
    ],
    "modelPreferences": {
      "hints": [
        {
          "name": "claude-3-sonnet"
        }
      ],
      "intelligencePriority": 0.8,
      "speedPriority": 0.5
    },
    "systemPrompt": "You are a helpful assistant.",
    "maxTokens": 100
  }
}
```

--------------------------------

### Sequence Diagram for Session Hijack

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/security_best_practices

This Mermaid sequence diagram illustrates the flow of a session hijack attack. It shows the interaction between a client, server, and attacker, highlighting how an attacker can obtain and use a session ID to impersonate the client.

```mermaid
sequenceDiagram
    participant Client
    participant Server
    participant Attacker

    Client->>Server: Initialize (login/authenticate)
    Server-->>Client: Respond with session ID (persistent session created)

    Attacker->>Server: Access/guess session ID
    Note right of Attacker: Attacker knows/guesses session ID

    Attacker->>Server: Make API call (using session ID, no re-auth)
    Server-->>Attacker: Respond as if Attacker is Client (session hijack)
```

--------------------------------

### MCP Completion Message Flow (Mermaid)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/completion

This sequence diagram illustrates the message flow for completion requests between a client and a server, showing how suggestions are exchanged as the user interacts.

```mermaid
sequenceDiagram
    participant Client
    participant Server

    Note over Client: User types argument
    Client->>Server: completion/complete
    Server-->>Client: Completion suggestions

    Note over Client: User continues typing
    Client->>Server: completion/complete
    Server-->>Client: Refined suggestions
```

--------------------------------

### Resource Title Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the optional 'title' property for a resource, optimized for human readability and end-user contexts. If not provided, the 'name' property should be used as a fallback.

```typescript
title?: string
```

--------------------------------

### JSON Text Content for LLM Messages

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Defines the structure for plain text messages within the LLM prompt protocol, including optional annotations for metadata.

```json
{
  "type": "text",
  "text": "The text content of the message"
}
```

--------------------------------

### Server Capabilities: Resource Management

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Indicates if the server offers resources to read and supports notifications for changes to the resource list, as well as subscribing to resource updates. This is an optional property.

```TypeScript
resources?: {
  listChanged?: boolean;
  subscribe?: boolean
}
```

--------------------------------

### Protocol Version

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Specifies the version of the Model Context Protocol that the client supports. The client may also support older versions, allowing for backward compatibility.

```typescript
protocolVersion: string;
```

--------------------------------

### Set Anthropic API Key Environment Variable

Source: https://modelcontextprotocol.io/docs/develop/build-client

This command shows how to set the `ANTHROPIC_API_KEY` environment variable in a bash shell. This key is required for authenticating with the Anthropic API when using the MCP client with Anthropic models.

```bash
export ANTHROPIC_API_KEY='your-anthropic-api-key-here'
```

--------------------------------

### Define ModelHint Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the ModelHint interface used for providing hints during LLM model selection. It includes an optional 'name' property for specifying model name hints.

```TypeScript
interface ModelHint {
  name?: string;
}
```

--------------------------------

### Configure Tool Permissions for Remote MCP Servers

Source: https://modelcontextprotocol.io/docs/develop/connect-remote-servers

Learn how to manage which tools Claude can access on remote MCP servers by configuring permissions in the connector settings. This ensures authorized actions and enhances security.

```N/A
Navigate back to the Connectors settings and click on your connected server. Here you can enable or disable specific tools, set usage limits, and configure other security parameters according to your needs.
```

--------------------------------

### JSON Image Content for LLM Messages

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Specifies the format for including image data in LLM messages, requiring base64 encoding and a MIME type for multi-modal interactions.

```json
{
  "type": "image",
  "data": "base64-encoded-image-data",
  "mimeType": "image/png"
}
```

--------------------------------

### Define Tool Name

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Specifies the name of the tool, intended for programmatic or logical use. It can also serve as a fallback display name if a title is not provided.

```typescript
name: string
```

--------------------------------

### Define ClientCapabilities Interface in TypeScript

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for client capabilities, including optional properties for elicitation, experimental features, and root management. It allows clients to declare support for specific protocol features.

```TypeScript
interface ClientCapabilities {
  elicitation?: object;
  experimental?: { [key: string]: object };
  roots?: {
    listChanged?: boolean;
  };
  sampling?: object;
}
```

--------------------------------

### Declare Completions Capability (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/completion

Servers must declare support for the 'completions' capability by including it in their capabilities object. This signals to clients that the server can provide autocompletion suggestions.

```json
{
  "capabilities": {
    "completions": {}
  }
}
```

--------------------------------

### Define Client Capabilities Sampling

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the optional 'sampling' capability for a client, indicating support for sampling from an LLM. This is represented as an object.

```typescript
interface ClientCapabilities {
  sampling?: object;
}
```

--------------------------------

### MCP Pagination Request Format

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/pagination

Illustrates the JSON format for a client request to continue pagination in the Model Context Protocol, including the 'cursor' obtained from a previous response.

```json
{
  "jsonrpc": "2.0",
  "id": "124",
  "method": "resources/list",
  "params": {
    "cursor": "eyJwYWdlIjogMn0="
  }
}
```

--------------------------------

### Authorization Server: User Consent for Static Client IDs

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP proxy servers using static client IDs must obtain user consent for each dynamically registered client before forwarding to third-party authorization servers.

```General
MCP proxy servers using static client IDs MUST obtain user consent for each dynamically registered client before forwarding to third-party authorization servers (which may require additional consent).
```

--------------------------------

### MCP Client: State Parameter Verification

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP clients should use and verify state parameters in the authorization code flow to mitigate open redirection risks.

```General
MCP clients SHOULD use and verify state parameters in the authorization code flow and discard any results that do not include or have a mismatch with the original state.
```

--------------------------------

### Define PromptReference Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines how to identify a prompt, including its programmatic name and an optional human-readable title. The type is fixed to "ref/prompt".

```typescript
interface PromptReference {
  name: string;
  title?: string;
  type: "ref/prompt";
}
```

--------------------------------

### Server Capabilities: Logging Support

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Indicates if the server supports sending log messages to the client. This is an optional property.

```TypeScript
logging?: object
```

--------------------------------

### Elicitation Request Schema Definition

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

Defines the structure for elicitation requests, limiting schemas to flat objects with primitive properties. This simplifies client implementation by specifying property types, titles, descriptions, and constraints like minimum/maximum values.

```json
"requestedSchema": {
  "type": "object",
  "properties": {
    "propertyName": {
      "type": "string",
      "title": "Display Name",
      "description": "Description of the property"
    },
    "anotherProperty": {
      "type": "number",
      "minimum": 0,
      "maximum": 100
    }
  },
  "required": ["propertyName"]
}
```

--------------------------------

### ListResourcesResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for the server's response to a client's resources/list request. It includes optional metadata, a cursor for pagination, and an array of resources. This interface inherits properties from PaginatedResult.

```TypeScript
interface ListResourcesResult {
  _meta?: { [key: string]: unknown };
  nextCursor?: string;
  resources: Resource[];
  [key: string]: unknown;
}
```

--------------------------------

### NWS API Request Helper Function (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-server

A helper function to make asynchronous requests to the National Weather Service (NWS) API. It includes proper error handling, sets the 'User-Agent' and 'Accept' headers, and returns the JSON response or None if an error occurs.

```Python
async def make_nws_request(url: str) -> dict[str, Any] | None:
    """Make a request to the NWS API with proper error handling."""
    headers = {
        "User-Agent": USER_AGENT,
        "Accept": "application/geo+json"
    }
    async with httpx.AsyncClient() as client:
        try:
            response = await client.get(url, headers=headers, timeout=30.0)
            response.raise_for_status()
            return response.json()
        except Exception:
            return None
```

--------------------------------

### Create Simple Text Elicitation Request

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

A server sends an 'elicitation/create' request with a message and a simple JSON schema for text input. The response contains the user's input or an action.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "elicitation/create",
  "params": {
    "message": "Please provide your GitHub username",
    "requestedSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": ["name"]
    }
  }
}
```

--------------------------------

### Define PromptMessage Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure of a message within a prompt, including its content and the role it plays. This interface is similar to SamplingMessage but allows for embedding resources from the MCP server.

```typescript
interface PromptMessage {
  content: ContentBlock;
  role: Role;
}
```

--------------------------------

### Declare MCP Prompts Capability

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Servers supporting prompts must declare the 'prompts' capability during initialization. The 'listChanged' flag indicates if notifications will be sent when the prompt list is updated.

```json
{
  "capabilities": {
    "prompts": {
      "listChanged": true
    }
  }
}
```

--------------------------------

### Define Sampling Message Parameters

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the optional parameters for a sampling message, including system prompt, temperature, and stop sequences. These parameters control the behavior and output of the language model.

```TypeScript
interface SamplingMessage {
    includeContext?: "none" | "thisServer" | "allServers";
    maxTokens: number;
    messages: SamplingMessage[];
    metadata?: object;
    modelPreferences?: ModelPreferences;
    stopSequences?: string[];
    systemPrompt?: string;
    temperature?: number;
}
```

--------------------------------

### MCP Sampling Message Flow

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/sampling

Visualizes the sequence of interactions between Server, Client, User, and LLM during an MCP sampling request, from initiation to response.

```mermaid
sequenceDiagram
    participant Server
    participant Client
    participant User
    participant LLM

    Note over Server,Client: Server initiates sampling
    Server->>Client: sampling/createMessage

    Note over Client,User: Human-in-the-loop review
    Client->>User: Present request for approval
    User-->>Client: Review and approve/modify

    Note over Client,LLM: Model interaction
    Client->>LLM: Forward approved request
    LLM-->>Client: Return generation

    Note over Client,User: Response review
    Client->>User: Present response for approval
    User-->>Client: Review and approve/modify

    Note over Server,Client: Complete request
    Client-->>Server: Return approved response
```

--------------------------------

### Making NWS API Requests (TypeScript)

Source: https://modelcontextprotocol.io/docs/develop/build-server

Provides a helper function `makeNWSRequest` to query the National Weather Service (NWS) API. It handles making `fetch` requests with appropriate headers (User-Agent, Accept) and error handling for network issues or non-successful HTTP responses.

```typescript
// Helper function for making NWS API requests
async function makeNWSRequest<T>(url: string): Promise<T | null> {
  const headers = {
    "User-Agent": USER_AGENT,
    "Accept": "application/geo+json",
  };

  try {
    const response = await fetch(url, { headers });
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    return (await response.json()) as T;
  } catch (error) {
    console.error("Error making NWS request:", error);
    return null;
  }
}
```

--------------------------------

### Resource Subscription Request

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

A JSON RPC request from a client to subscribe to changes for a specific resource. This allows clients to receive update notifications.

```json
{
  "jsonrpc": "2.0",
  "id": 4,
  "method": "resources/subscribe",
  "params": {
    "uri": "file:///project/src/main.rs"
  }
}
```

--------------------------------

### Define TextResourceContents Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the TextResourceContents interface, which extends ResourceContents and specifies properties for text-based resources. It includes optional metadata (_meta), an optional mimeType, a mandatory text property, and a mandatory uri.

```TypeScript
interface TextResourceContents {
  _meta?: { [key: string]: unknown };
  mimeType?: string;
  text: string;
  uri: string;
}
```

--------------------------------

### MCP Initialized Notification

Source: https://modelcontextprotocol.io/docs/learn/architecture

A JSON-RPC 2.0 notification sent by the client to the server after successful initialization, indicating readiness for further communication.

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/initialized"
}
```

--------------------------------

### Logging Message Flow (Mermaid)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/logging

A sequence diagram illustrating the message flow for logging in the Model Context Protocol, including setting the log level and receiving log message notifications.

```mermaid
sequenceDiagram
    participant Client
    participant Server

    Note over Client,Server: Configure Logging
    Client->>Server: logging/setLevel (info)
    Server-->>Client: Empty Result

    Note over Client,Server: Server Activity
    Server--)Client: notifications/message (info)
    Server--)Client: notifications/message (warning)
    Server--)Client: notifications/message (error)

    Note over Client,Server: Level Change
    Client->>Server: logging/setLevel (error)
    Server-->>Client: Empty Result
    Note over Server: Only sends error level<br/>and above
```

--------------------------------

### ProgressNotification Params Type Declaration

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Details the parameters for the ProgressNotification, specifying types and optionality for message, progress, progressToken, and total.

```typescript
params: {
  message?: string;
  progress: number;
  progressToken: ProgressToken;
  total?: number;
}
```

--------------------------------

### Define Resource Interface in TypeScript

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for a 'Resource' object. It includes optional properties for metadata, annotations, description, MIME type, size, and title, along with mandatory 'name' and 'uri' properties. The '_meta' property allows for arbitrary key-value string metadata, and 'annotations' can hold custom 'Annotations'.

```typescript
interface Resource {
  _meta?: { [key: string]: unknown };
  annotations?: Annotations;
  description?: string;
  mimeType?: string;
  name: string;
  size?: number;
  title?: string;
  uri: string;
}
```

--------------------------------

### MCP Pagination Response Format

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/pagination

Demonstrates the JSON structure for a paginated response in the Model Context Protocol, including the list of resources and an optional 'nextCursor' for subsequent requests.

```json
{
  "jsonrpc": "2.0",
  "id": "123",
  "result": {
    "resources": [...],
    "nextCursor": "eyJwYWdlIjogM30="
  }
}
```

--------------------------------

### Define CompleteResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the `CompleteResult` interface, which represents the server's response to a completion or complete request. It includes optional metadata and a completion object containing completion values, and potentially flags for more results.

```typescript
interface CompleteResult {
  _meta?: { [key: string]: unknown };
  completion: {
    hasMore?: boolean;
    total?: number;
    values: string[];
  };
  [key: string]: unknown;
}
```

--------------------------------

### Define ReadResourceResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure of the response from a resources/read request. It includes optional metadata and an array of contents, which can be either TextResourceContents or BlobResourceContents.

```typescript
interface ReadResourceResult {
  _meta?: { [key: string]: unknown };
  contents: (TextResourceContents | BlobResourceContents)[];
  [key: string]: unknown;
}
```

--------------------------------

### MCP Roots Message Flow

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/roots

A Mermaid diagram illustrating the message flow for Model Context Protocol roots, showing server-client interactions for listing roots and handling changes.

```mermaid
sequenceDiagram
    participant Server
    participant Client

    Note over Server,Client: Discovery
    Server->>Client: roots/list
    Client-->>Server: Available roots

    Note over Server,Client: Changes
    Client--)Server: notifications/roots/list_changed
    Server->>Client: roots/list
    Client-->>Server: Updated roots
```

--------------------------------

### Define ResourceTemplateReference with URI

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines a ResourceTemplateReference, which is a reference to a resource or resource template definition. It includes a URI or URI template.

```typescript
interface ResourceTemplateReference {
  type: "ref/resource";
  uri: string;
}
```

--------------------------------

### Request Updated Tools List (JSON-RPC)

Source: https://modelcontextprotocol.io/docs/learn/architecture

A JSON-RPC 2.0 request sent by an MCP client to a server to retrieve the current list of available tools. This is typically sent in response to a 'tools/list_changed' notification.

```json
{
  "jsonrpc": "2.0",
  "id": 4,
  "method": "tools/list"
}
```

--------------------------------

### ModelHint Name Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Details the optional 'name' property within the ModelHint interface. This property serves as a hint for model selection, with specific guidelines on substring matching and potential cross-provider mapping.

```TypeScript
name?: string;
```

--------------------------------

### Define CompleteRequest Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the CompleteRequest interface, which represents a client request to the server for completion options. It includes the method, parameters (argument, context, ref), and their respective types.

```typescript
interface CompleteRequest {
  method: "completion/complete";
  params: {
    argument: { name: string; value: string };
    context?: { arguments?: { [key: string]: string } };
    ref: PromptReference | ResourceTemplateReference;
  };
}
```

--------------------------------

### Mermaid Sequence Diagram for MCP Interactions

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

A sequence diagram visualizing the flow of messages between a client and server during MCP session initialization and subsequent requests.

```mermaid
sequenceDiagram
    participant Client
    participant Server

    note over Client, Server: initialization

    Client->>+Server: POST InitializeRequest
    Server->>-Client: InitializeResponse<br>Mcp-Session-Id: 1868a90c...

    Client->>+Server: POST InitializedNotification<br>Mcp-Session-Id: 1868a90c...
    Server->>-Client: 202 Accepted

    note over Client, Server: client requests
    Client->>+Server: POST ... request ...<br>Mcp-Session-Id: 1868a90c...

    alt single HTTP response
      Server->>Client: ... response ...
    else server opens SSE stream
      loop while connection remains open
          Server-)Client: ... SSE messages from server ...
      end
      Server-)Client: SSE event: ... response ...
    end
    deactivate Server

    note over Client, Server: client notifications/responses
    Client->>+Server: POST ... notification/response ...<br>Mcp-Session-Id: 1868a90c...
    Server->>-Client: 202 Accepted

    note over Client, Server: server requests
    Client->>+Server: GET<br>Mcp-Session-Id: 1868a90c...
    loop while connection remains open
        Server-)Client: ... SSE messages from server ...
    end
    deactivate Server
```

--------------------------------

### Declare Sampling Capability

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/sampling

Clients supporting sampling must declare the 'sampling' capability during initialization by including it in the capabilities object.

```json
{
  "capabilities": {
    "sampling": {}
  }
}
```

--------------------------------

### MCP Client: PKCE Implementation

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP clients must implement PKCE (Proof Key for Code Exchange) according to OAuth 2.1 Section 7.5.2 to prevent authorization code interception and injection attacks.

```General
MCP clients MUST implement PKCE according to [OAuth 2.1 Section 7.5.2](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-7.5.2).
```

--------------------------------

### MCP Notification for Tool List Change

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

Servers that support 'listChanged' capability send a 'notifications/tools/list_changed' notification when the available tools change.

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/tools/list_changed"
}
```

--------------------------------

### Define Tool Title for UI

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Provides an optional human-readable title for a tool, intended for UI display. If not provided, the tool's name is used, with precedence given to annotations.title for Tool types.

```TypeScript
title?: string;
```

--------------------------------

### MCP Response for Reading Resource Contents

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

This JSON snippet shows the response structure from an MCP server when a client requests to read a resource's content. It includes metadata about the resource and its text content.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "contents": [
      {
        "uri": "file:///project/src/main.rs",
        "name": "main.rs",
        "title": "Rust Software Application Main File",
        "mimeType": "text/x-rust",
        "text": "fn main() {
    println!(\"Hello world!\");
}"
      }
    ]
  }
}
```

--------------------------------

### Define ResourceLink Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for a ResourceLink, which represents a resource accessible by the server. It includes properties for metadata, annotations, description, MIME type, name, size, title, type, and URI.

```TypeScript
interface ResourceLink {
  _meta?: { [key: string]: unknown };
  annotations?: Annotations;
  description?: string;
  mimeType?: string;
  name: string;
  size?: number;
  title?: string;
  type: "resource_link";
  uri: string;
}
```

--------------------------------

### Define CallToolRequest Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for a request to call a tool. It specifies the method as 'tools/call' and includes parameters for the tool's name and optional arguments.

```typescript
interface CallToolRequest {
  method: "tools/call";
  params: {
    arguments?: { [key: string]: unknown };
    name: string;
  };
}
```

--------------------------------

### Define ImageContent Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the ImageContent interface for representing image data. It includes optional metadata and annotations, and required base64-encoded data and MIME type.

```typescript
interface ImageContent {
  _meta?: { [key: string]: unknown };
  annotations?: Annotations;
  data: string;
  mimeType: string;
  type: "image";
}
```

--------------------------------

### Data Class for Weather Alerts API Response

Source: https://modelcontextprotocol.io/docs/develop/build-server

Defines the data structure for the 'Alert' response, containing a list of 'Feature' objects. Each feature includes properties like event, area description, severity, and instructions for the alert.

```kotlin
@Serializable
data class Alert(
    val features: List<Feature>
)

@Serializable
data class Alert.Feature(
    val properties: Properties
)

@Serializable
data class Alert.Properties(
    val event: String, val areaDesc: String, val severity: String,
    val description: String, val instruction: String?,
)
```

--------------------------------

### Communication Security: Redirect URI Requirements

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

All redirect URIs must be either 'localhost' or use HTTPS to prevent security vulnerabilities.

```General
All redirect URIs MUST be either `localhost` or use HTTPS.
```

--------------------------------

### Anthropic Messages API Interaction (TypeScript)

Source: https://modelcontextprotocol.io/docs/develop/build-client

This snippet demonstrates how to interact with the Anthropic Messages API to send a query and retrieve a text response. It handles model selection, token limits, and formats the response content.

```typescript
          const response = await this.anthropic.messages.create({
            model: "claude-3-5-sonnet-20241022",
            max_tokens: 1000,
            messages,
          });

          finalText.push(
            response.content[0].type === "text" ? response.content[0].text : ""
          );
        }
      }

      return finalText.join("\n");
    }
```

--------------------------------

### Define ContentBlock Type

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the 'ContentBlock' type as a union of various content types, including TextContent, ImageContent, AudioContent, ResourceLink, and EmbeddedResource.

```typescript
type ContentBlock = |
  TextContent |
  ImageContent |
  AudioContent |
  ResourceLink |
  EmbeddedResource;
```

--------------------------------

### C# HttpClient Extension for JSON

Source: https://modelcontextprotocol.io/docs/develop/build-server

A C# extension method for HttpClient to simplify reading JSON responses from API requests.

```csharp
using System.Text.Json;

internal static class HttpClientExt
{
    public static async Task<JsonDocument> ReadJsonDocumentAsync(this HttpClient client, string requestUri)
    {
        using var response = await client.GetAsync(requestUri);
        response.EnsureSuccessStatusCode();
        return await JsonDocument.ParseAsync(await response.Content.ReadAsStreamAsync());
    }
}
```

--------------------------------

### MCP Client Resource Parameter Usage

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP clients must implement and use the 'resource' parameter as defined in RFC 8707 to explicitly specify the target resource for token requests. This ensures tokens are bound to intended resources and prevents misuse.

```text
MCP clients MUST implement and use the `resource` parameter as defined in [RFC 8707 - Resource Indicators for OAuth 2.0](https://www.rfc-editor.org/rfc/rfc8707.html) to explicitly specify the target resource for which the token is being requested. This requirement aligns with the recommendation in [RFC 9728 Section 7.4](https://datatracker.ietf.org/doc/html/rfc9728#section-7.4). This ensures that access tokens are bound to their intended resources and cannot be misused across different services.
```

--------------------------------

### Refined Completion Response (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/completion

After receiving context, the server provides refined completion suggestions based on the provided argument values.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "completion": {
      "values": ["flask"],
      "total": 1,
      "hasMore": false
    }
  }
}
```

--------------------------------

### Set Log Level Request (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/logging

Clients can configure the minimum log level by sending a 'logging/setLevel' JSON-RPC request. The request includes a 'level' parameter specifying the desired minimum severity.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "logging/setLevel",
  "params": {
    "level": "info"
  }
}
```

--------------------------------

### InitializeResult _meta Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Describes the optional '_meta' property within the InitializeResult interface. This property can hold arbitrary key-value pairs of unknown type, intended for additional metadata. It is inherited from the 'Result' type.

```typescript
  _meta?: { [key: string]: unknown };
```

--------------------------------

### Resource MimeType Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the optional 'mimeType' property for a resource, indicating its MIME type if known. This helps in identifying the type of resource content.

```typescript
mimeType?: string
```

--------------------------------

### Resource Name Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the mandatory 'name' property for a resource, intended for programmatic or logical use, and can serve as a fallback display name.

```typescript
name: string
```

--------------------------------

### MCP Response for Tool Call Result

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/tools

The 'tools/call' response contains the result of the tool invocation, including content and an 'isError' flag. Content can be structured or unstructured.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Current weather in New York:\nTemperature: 72°F\nConditions: Partly cloudy"
      }
    ],
    "isError": false
  }
}
```

--------------------------------

### Define ToolAnnotations Interface in TypeScript

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines optional properties for annotating tool behavior. It includes hints for destructive operations, idempotency, read-only status, and open-world interactions, along with an optional title. These annotations are intended as hints and should be used cautiously.

```TypeScript
interface ToolAnnotations {
  destructiveHint?: boolean;
  idempotentHint?: boolean;
  openWorldHint?: boolean;
  readOnlyHint?: boolean;
  title?: string;
}
```

--------------------------------

### tsconfig.json Configuration for MCP Weather Server

Source: https://modelcontextprotocol.io/docs/develop/build-server

Defines the TypeScript compiler options for the MCP weather server project. It specifies target ECMAScript version, module system, output directory, root directory, strictness, and other essential configurations for compiling the TypeScript code.

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "outDir": "./build",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
```

--------------------------------

### Request Roots List

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/roots

Servers request the list of available roots from clients using the 'roots/list' method. This is a standard JSON-RPC request.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "roots/list"
}
```

--------------------------------

### Define ReadResourceRequest Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for a `ReadResourceRequest`. It specifies the request method as 'resources/read' and includes a 'params' object with a 'uri' property of type string. The 'uri' parameter represents the resource to be read and can be interpreted by the server.

```typescript
interface ReadResourceRequest {
  method: "resources/read";
  params: {
    uri: string;
  };
}
```

--------------------------------

### Send Logging Messages to Client

Source: https://modelcontextprotocol.io/docs/learn/architecture

MCP servers can send log messages to clients for debugging and monitoring purposes using the 'logging/log' method. This facilitates tracking the server's operational status and identifying potential issues.

```JSON-RPC
{
  "method": "logging/log",
  "params": {
    "level": "info",
    "message": "Processing request..."
  },
  "jsonrpc": "2.0"
}
```

--------------------------------

### Define ProgressNotification Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for progress notifications, including method, message, progress, progressToken, and total.

```typescript
interface ProgressNotification {
  method: "notifications/progress";
  params?: {
    message?: string;
    progress: number;
    progressToken: ProgressToken;
    total?: number;
  };
}
```

--------------------------------

### Response to Simple Text Elicitation Request

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

The server receives a response to the elicitation request, which includes the user's accepted content or a decline action.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "action": "accept",
    "content": {
      "name": "octocat"
    }
  }
}
```

--------------------------------

### Pagination Response Format

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/pagination

Describes the structure of a paginated response, including the current page of results and an optional 'nextCursor' field.

```APIDOC
## Response

### Success Response (200)

- **jsonrpc** (string) - The JSON-RPC protocol version.
- **id** (string) - The request ID.
- **result** (object) - Contains the results of the operation.
  - **resources** (array) - The current page of results.
  - **nextCursor** (string) - An opaque token for the next page of results. Omitted if no more results exist.
```

--------------------------------

### Define InitializedNotification Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure of the InitializedNotification. It includes a fixed 'method' property and an optional 'params' object that can contain metadata.

```typescript
interface InitializedNotification {
  method: "notifications/initialized";
  params?: {
    _meta?: {
      [key: string]: unknown;
    };
    [key: string]: unknown;
  };
}
```

--------------------------------

### Define JSONRPCNotification Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for a JSON-RPC notification, including the version, method, and optional parameters. The params can include metadata.

```TypeScript
interface JSONRPCNotification {
  jsonrpc: "2.0";
  method: string;
  params?: {
    _meta?: {
      [key: string]: string;
    };
    [key: string]: unknown;
  };
}
```

--------------------------------

### Define String Schema Properties

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for a string schema, including optional properties for description, format (uri, email, date, date-time), maxLength, and minLength, along with a mandatory type property set to 'string'.

```TypeScript
interface StringSchema {
  description?: string;
  format?: "uri" | "email" | "date" | "date-time";
  maxLength?: number;
  minLength?: number;
  title?: string;
  type: "string";
}
```

--------------------------------

### Create Structured Data Elicitation Request

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

Servers can request structured data using a more complex JSON schema, defining multiple properties with types, formats, and constraints. The response includes the structured user data or a decline/cancel action.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "elicitation/create",
  "params": {
    "message": "Please provide your contact information",
    "requestedSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Your full name"
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "Your email address"
        },
        "age": {
          "type": "number",
          "minimum": 18,
          "description": "Your age"
        }
      },
      "required": ["name", "email"]
    }
  }
}
```

--------------------------------

### Data Class for Weather Points API Response

Source: https://modelcontextprotocol.io/docs/develop/build-server

Defines the data structure for the 'Points' response from the weather API, containing properties that include the URL for the detailed forecast.

```kotlin
@Serializable
data class Points(
    val properties: Properties
) {
    @Serializable
    data class Properties(val forecast: String)
}
```

--------------------------------

### Progress Notification (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/utilities/progress

This JSON structure shows the format for progress notifications, including the `progressToken`, current `progress`, optional `total`, and an optional `message`.

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/progress",
  "params": {
    "progressToken": "abc123",
    "progress": 50,
    "total": 100,
    "message": "Reticulating splines..."
  }
}
```

--------------------------------

### Notify Tools List Change (JSON-RPC)

Source: https://modelcontextprotocol.io/docs/learn/architecture

A JSON-RPC 2.0 notification sent by MCP servers to clients when the list of available tools has changed. This allows clients to proactively update their tool registry without polling.

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/tools/list_changed"
}
```

--------------------------------

### MCP Malicious OAuth Proxy Usage Sequence Diagram

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/security_best_practices

Demonstrates a malicious attack scenario where an MCP proxy server's usage is exploited to bypass user consent. This flow highlights how an attacker can leverage existing cookies to obtain authorization codes and impersonate users.

```mermaid
sequenceDiagram
    participant UA as User-Agent (Browser)
    participant M as MCP Proxy Server
    participant TAS as Third-Party Authorization Server
    participant A as Attacker


    Note over UA,A: Step 2: Attack (leveraging existing cookie, skipping consent)
    A->>M: Dynamically register malicious client, redirect_uri: attacker.com
    A->>UA: Sends malicious link
    UA->>TAS: Authorization request (client_id: mcp-proxy) + consent cookie
    rect rgba(255, 17, 0, 0.67)
    TAS->>TAS: Cookie present, consent skipped
    end

   TAS->>UA: 3P Authorization code + redirect to mcp-proxy-server.com
   UA->>M: 3P Authorization code
   Note over M,TAS: Exchange 3P code for 3P token
   Note over M: Generate MCP authorization code
   M->>UA: Redirect to attacker.com with MCP Authorization code
   UA->>A: MCP Authorization code delivered to attacker.com
   Note over M,A: Attacker exchanges MCP code for MCP token
   A->>M: Attacker impersonates user to MCP server
```

--------------------------------

### Declare Logging Capability (JSON)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/utilities/logging

Servers must declare the 'logging' capability in their capabilities declaration to indicate support for the logging protocol. This is a JSON object.

```json
{
  "capabilities": {
    "logging": {}
  }
}
```

--------------------------------

### Notify Prompt List Change (MCP)

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/prompts

Servers that declared 'listChanged' capability should send a 'notifications/prompts/list_changed' notification when the available prompts change.

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/prompts/list_changed"
}
```

--------------------------------

### MCP JSON-RPC Request Structure

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/index

Defines the structure for requests sent within the Model Context Protocol, adhering to JSON-RPC 2.0. Requests must include a non-null, unique ID and a method name.

```TypeScript
{
  jsonrpc: "2.0";
  id: string | number;
  method: string;
  params?: {
    [key: string]: unknown;
  };
}
```

--------------------------------

### Dynamic Client Registration (RFC 7591)

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP clients and authorization servers should support the OAuth 2.0 Dynamic Client Registration Protocol (RFC 7591) for automatic client registration without user interaction. This is crucial for MCP clients that may not know all potential servers in advance, reducing friction and enabling seamless connections.

```text
MCP clients and authorization servers SHOULD support the OAuth 2.0 Dynamic Client Registration Protocol [RFC7591](https://datatracker.ietf.org/doc/html/rfc7591) to allow MCP clients to obtain OAuth client IDs without user interaction.
```

--------------------------------

### Define Result Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the `Result` interface, which represents a response object. It includes an optional `_meta` property for metadata and allows for arbitrary string-keyed properties of unknown type.

```TypeScript
interface Result {
  _meta?: { [key: string]: unknown };
  [key: string]: unknown;
}
```

--------------------------------

### Format NWS Alert Function (Python)

Source: https://modelcontextprotocol.io/docs/develop/build-server

A utility function to format alert data received from the NWS API into a human-readable string. It extracts key properties like event, area, severity, description, and instructions from the alert feature.

```Python
def format_alert(feature: dict) -> str:
    """Format an alert feature into a readable string."""
    props = feature["properties"]
    return f"""
Event: {props.get('event', 'Unknown')}
Area: {props.get('areaDesc', 'Unknown')}
Severity: {props.get('severity', 'Unknown')}
Description: {props.get('description', 'No description available')}
Instructions: {props.get('instruction', 'No specific instructions provided')}
"""
```

--------------------------------

### MCP Request to Read Resource Contents

Source: https://modelcontextprotocol.io/specification/2025-06-18/server/resources

This JSON snippet displays the JSON-RPC request format used by an MCP client to read the contents of a specific resource, identified by its URI.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "resources/read",
  "params": {
    "uri": "file:///project/src/main.rs"
  }
}
```

--------------------------------

### Define size Property (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the optional size property for a resource, representing its raw content size in bytes. This is a number type.

```typescript
/**
 * The size of the raw resource content, in bytes (i.e., before base64 encoding or any tokenization), if known.
 *
 * This can be used by Hosts to display file sizes and estimate context window usage.
 */
size?: number;
```

--------------------------------

### Define Tool Output Schema

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure of a tool's output using a JSON Schema. This schema is used to validate the structuredContent field in a CallToolResult.

```TypeScript
schema?: object;
```

--------------------------------

### Data Class for Weather Forecast API Response

Source: https://modelcontextprotocol.io/docs/develop/build-server

Defines the data structure for the 'Forecast' response, including a list of 'Period' objects, each containing detailed weather information like temperature, wind, and forecast description.

```kotlin
@Serializable
data class Forecast(
    val properties: Properties
)

@Serializable
data class Forecast.Properties(val periods: List<Period>)

@Serializable
data class Forecast.Period(
    val number: Int, val name: String, val startTime: String, val endTime: String,
    val isDaytime: Boolean, val temperature: Int, val temperatureUnit: String,
    val temperatureTrend: String, val probabilityOfPrecipitation: JsonObject,
    val windSpeed: String, val windDirection: String,
    val shortForecast: String, val detailedForecast: String,
)
```

--------------------------------

### Define CallToolResult Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the CallToolResult interface used to structure the server's response to a tool call. It includes optional metadata (_meta), an array of ContentBlock objects, and an optional boolean flag (isError) to indicate success or failure.

```TypeScript
interface CallToolResult {
  _meta?: { [key: string]: unknown };
  content: ContentBlock[];
  isError?: boolean;
  structuredContent?: { [key: string]: unknown };
  [key: string]: unknown;
}
```

--------------------------------

### Notify Clients of Tool Updates

Source: https://modelcontextprotocol.io/docs/learn/architecture

MCP servers can send JSON-RPC 2.0 notification messages to inform connected clients about changes in available tools. This ensures clients have up-to-date information on server capabilities.

```JSON-RPC
{
  "method": "tools/update",
  "params": {
    "added": ["new_tool"],
    "removed": ["old_tool"]
  },
  "jsonrpc": "2.0"
}
```

--------------------------------

### CompleteResult _meta Property

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Details the optional `_meta` property within the `CompleteResult` interface. This property can hold arbitrary string-keyed metadata, as described in the general fields documentation.

```typescript
_meta?: { [key: string]: unknown };
```

--------------------------------

### Declare Roots Capability

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/roots

Clients supporting roots must declare the 'roots' capability during initialization. The 'listChanged' property indicates if the client will emit notifications when the root list changes.

```json
{
  "capabilities": {
    "roots": {
      "listChanged": true
    }
  }
}
```

--------------------------------

### Subscribe to Resource Updates (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the `SubscribeRequest` interface used to request notifications from the server when a specific resource changes. It requires a method identifier and parameters including the URI of the resource to monitor.

```typescript
interface SubscribeRequest {
  method: "resources/subscribe";
  params: { uri: string };
}
```

--------------------------------

### ModelPreferences: Cost Priority (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Specifies the priority for cost when selecting a model. A value of 0 indicates cost is unimportant, while 1 signifies that cost is the primary consideration. This property helps clients balance model capabilities against their associated costs.

```typescript
costPriority?: number;
```

--------------------------------

### MCP Client: Resource Parameter in Authorization Requests

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP clients must include the 'resource' parameter in authorization and token requests to bind tokens to their intended audiences, enhancing security when supported by the Authorization Server.

```General
MCP clients MUST include the `resource` parameter in authorization and token requests.
```

--------------------------------

### Response to Structured Data Elicitation Request

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

The server receives a structured response from the user, including accepted content with multiple fields, or a decline/cancel action.

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "action": "accept",
    "content": {
      "name": "Monalisa Octocat",
      "email": "octocat@github.com",
      "age": 30
    }
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "action": "decline"
  }
}
```

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "action": "cancel"
  }
}
```

--------------------------------

### Define SamplingMessage Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Describes a message exchanged with an LLM API. It contains content, which can be text, image, or audio, and a role indicating the sender or receiver.

```typescript
interface SamplingMessage {
  content: TextContent | ImageContent | AudioContent;
  role: Role;
}
```

--------------------------------

### Define title Property (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the optional title property for a resource, intended for UI and end-user contexts for human-readable display. This is a string type.

```typescript
/**
 * Intended for UI and end-user contexts — optimized to be human-readable and easily understood,
 * even by those unfamiliar with domain-specific terminology.
 *
 * If not provided, the name should be used for display (except for Tool,
 * where `annotations.title` should be given precedence over using `name`,
 * if present).
 */
title?: string;
```

--------------------------------

### Define Number Schema

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines a schema for numbers, including optional properties like description, minimum, and maximum values. The 'type' property must be either 'number' or 'integer'.

```typescript
interface NumberSchema {
  description?: string;
  maximum?: number;
  minimum?: number;
  title?: string;
  type: "number" | "integer";
}
```

--------------------------------

### Define LoggingMessageNotification Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for logging message notifications, including the method type, logging parameters (data, level, and an optional logger), and a description of its purpose.

```typescript
interface LoggingMessageNotification {
  method: "notifications/message";
  params: {
    data: unknown;
    level: LoggingLevel;
    logger?: string;
  };
}
```

--------------------------------

### Define RootsListChangedNotification Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for the `RootsListChangedNotification`. It includes a fixed `method` property and an optional `params` property that can hold metadata or other key-value pairs.

```typescript
interface RootsListChangedNotification {
  method: "notifications/roots/list_changed";
  params?: {
    _meta?: {
      [key: string]: unknown;
    };
    [key: string]: unknown;
  };
}
```

--------------------------------

### Define PromptArgument Interface in TypeScript

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for a prompt argument. It includes properties for description, name, required status, and title, allowing for detailed specification of prompt inputs.

```TypeScript
interface PromptArgument {
  description?: string;
  name: string;
  required?: boolean;
  title?: string;
}
```

--------------------------------

### Sampling Message Response

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/sampling

The response to a 'sampling/createMessage' request contains the generated content, the model used, and the stop reason.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "role": "assistant",
    "content": {
      "type": "text",
      "text": "The capital of France is Paris."
    },
    "model": "claude-3-sonnet-20240307",
    "stopReason": "endTurn"
  }
}
```

--------------------------------

### MCP Client Authorization Header

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

Demonstrates the required format for the Authorization header in HTTP requests from an MCP client to an MCP server, using a Bearer token as defined in OAuth 2.1.

```http
Authorization: Bearer <access-token>
```

--------------------------------

### Define EmbeddedResource Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the `EmbeddedResource` interface for embedding resource content within LLM interactions. It includes optional metadata (`_meta`), optional annotations, the resource content itself (either text or blob), and a type identifier.

```typescript
interface EmbeddedResource {
  _meta?: { [key: string]: unknown };
  annotations?: Annotations;
  resource: TextResourceContents | BlobResourceContents;
  type: "resource";
}
```

--------------------------------

### JSON-RPC Error Response Format

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/sampling

Illustrates the standard JSON-RPC error response format. This structure is used by clients to report common failure cases during LLM interactions, including a unique error code and a descriptive message.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -1,
    "message": "User rejected sampling request"
  }
}
```

--------------------------------

### Define Cursor Type

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the 'Cursor' type as an opaque string token used for pagination. This allows clients to navigate through results.

```typescript
type Cursor = string;
```

--------------------------------

### Declare Elicitation Capability

Source: https://modelcontextprotocol.io/specification/2025-06-18/client/elicitation

Clients must declare the 'elicitation' capability during initialization to support this feature. This is a JSON object indicating the client's support for elicitation.

```json
{
  "capabilities": {
    "elicitation": {}
  }
}
```

--------------------------------

### Server Response to Accepted JSON-RPC Request

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

Illustrates the expected server response when it successfully accepts a JSON-RPC request from the client. This typically involves a 202 Accepted status with no response body.

```HTTP
HTTP/1.1 202 Accepted
Content-Type: text/event-stream


```

--------------------------------

### Define PromptListChangedNotification Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for the PromptListChangedNotification. It includes a 'method' property to identify the notification type and an optional 'params' object for additional data, which can include metadata or custom key-value pairs.

```typescript
interface PromptListChangedNotification {
  method: "notifications/prompts/list_changed";
  params?: {
    _meta?: {
      [key: string]: unknown;
    };
    [key: string]: unknown;
  };
}
```

--------------------------------

### ToolListChangedNotification Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for a notification indicating that the list of tools has changed. It includes a 'method' property to identify the notification type and an optional 'params' object for additional data, which can include metadata.

```TypeScript
interface ToolListChangedNotification {
  method: "notifications/tools/list_changed";
  params?: {
    _meta?: { [key: string]: unknown };
    [key: string]: unknown;
  };
}
```

--------------------------------

### Define CreateMessageRequest Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the TypeScript interface for CreateMessageRequest, used to send requests for LLM message sampling. It specifies the method, maximum tokens, messages, and optional parameters like context inclusion and stop sequences.

```typescript
interface CreateMessageRequest {
  method: "sampling/createMessage";
  params: {
    includeContext?: "none" | "thisServer" | "allServers";
    maxTokens: number;
    messages: SamplingMessage[];
    metadata?: object;
    modelPreferences?: ModelPreferences;
    stopSequences?: string[];
    systemPrompt?: string;
    temperature?: number;
  };
}
```

--------------------------------

### Define PingRequest Interface in TypeScript

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

This TypeScript interface defines the structure for a PingRequest. It includes a mandatory 'method' property set to 'ping' and an optional 'params' property. The 'params' can contain a '_meta' object with a 'progressToken' and other index signatures for arbitrary string keys mapping to unknown types.

```typescript
interface PingRequest {
  method: "ping";
  params?: {
    _meta?: {
      progressToken?: ProgressToken;
      [key: string]: unknown;
    };
    [key: string]: unknown;
  };
}
```

--------------------------------

### Define ResourceUpdatedNotification Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the structure for a ResourceUpdatedNotification, including the method type and parameters. This interface is used to notify clients about resource updates.

```typescript
interface ResourceUpdatedNotification {
  method: "notifications/resources/updated";
  params: { uri: string };
}
```

--------------------------------

### MCP Server Metadata Discovery (RFC 8414)

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP clients must follow the OAuth 2.0 Authorization Server Metadata specification (RFC 8414) to obtain necessary information for interacting with the authorization server. This involves discovering the server's metadata endpoint.

```text
MCP clients MUST follow the OAuth 2.0 Authorization Server Metadata [RFC8414](https://datatracker.ietf.org/doc/html/rfc8414) specification to obtain the information required to interact with the authorization server.
```

--------------------------------

### Define uri Property (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the mandatory uri property for a resource, specifying its Uniform Resource Identifier. This is a string type.

```typescript
/**
 * The URI of this resource.
 */
uri: string;
```

--------------------------------

### ResourceUpdatedNotification Params - URI

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Specifies the 'params' property for ResourceUpdatedNotification, which contains the URI of the updated resource. This URI might refer to a sub-resource.

```typescript
params: {
  uri: string;
}
```

--------------------------------

### Client Sending JSON-RPC Request via HTTP POST

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports

Details the requirements for a client to send a JSON-RPC message to the MCP endpoint using an HTTP POST request. This includes setting the correct headers and body format.

```HTTP
POST /mcp-endpoint HTTP/1.1
Host: your-server.com
Accept: application/json, text/event-stream
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "method": "your_method",
  "params": {},
  "id": 1
}
```

--------------------------------

### MCP Authorization: WWW-Authenticate Header

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

MCP servers must use the 'WWW-Authenticate' HTTP header when returning a 401 Unauthorized response to indicate the location of the resource server metadata URL. MCP clients must be able to parse this header.

```HTTP
HTTP/1.1 401 Unauthorized
WWW-Authenticate: Bearer realm="example", authorization_servers="https://auth.example.com/oauth2/"

```

--------------------------------

### Format Weather Alert Data

Source: https://modelcontextprotocol.io/docs/develop/build-server

Formats weather alert data into a human-readable string. It extracts properties like event, area, severity, status, and headline from an AlertFeature object. Dependencies include the AlertFeature interface.

```typescript
interface AlertFeature {
  properties: {
    event?: string;
    areaDesc?: string;
    severity?: string;
    status?: string;
    headline?: string;
  };
}

// Format alert data
function formatAlert(feature: AlertFeature): string {
  const props = feature.properties;
  return [
    `Event: ${props.event || "Unknown"}`,
    `Area: ${props.areaDesc || "Unknown"}`,
    `Severity: ${props.severity || "Unknown"}`,
    `Status: ${props.status || "Unknown"}`,
    `Headline: ${props.headline || "No headline"}`,
    "---",
  ].join("\n");
}
```

--------------------------------

### Set Logging Level Request Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the interface for a request to set the logging level. It includes the method name and parameters, specifically the desired logging level.

```typescript
interface SetLevelRequest {
  method: "logging/setLevel";
  params: { level: LoggingLevel };
}
```

--------------------------------

### Define Annotations Interface (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the Annotations interface, which includes optional properties for audience, lastModified, and priority. This interface is used to provide metadata for client-side usage or display of objects.

```typescript
interface Annotations {
  audience?: Role[];
  lastModified?: string;
  priority?: number;
}
```

--------------------------------

### Authorization Server: Exact Redirect URI Validation

Source: https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization

Authorization servers must validate exact redirect URIs against pre-registered values to prevent open redirection attacks.

```General
Authorization servers MUST validate exact redirect URIs against pre-registered values to prevent redirection attacks.
```

--------------------------------

### LoggingLevel Type Alias

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the possible severity levels for log messages. These levels correspond to syslog message severities as defined in RFC-5424, providing a standardized way to categorize log entries.

```TypeScript
type LoggingLevel =
  | "debug"
  | "info"
  | "notice"
  | "warning"
  | "error"
  | "critical"
  | "alert"
  | "emergency"
```

--------------------------------

### Define ResourceListChangedNotification Interface

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Defines the TypeScript interface for ResourceListChangedNotification. This interface represents a notification sent from the server to the client indicating that the list of resources has changed. It includes a 'method' property to identify the notification type and an optional 'params' property for additional data, which can include metadata.

```TypeScript
interface ResourceListChangedNotification {
  method: "notifications/resources/list_changed";
  params?: {
    _meta?: {
      [key: string]: unknown;
    };
    [key: string]: unknown;
  };
}
```

--------------------------------

### Annotations Priority Property (TypeScript)

Source: https://modelcontextprotocol.io/specification/2025-06-18/schema

Explains the 'priority' property within the Annotations interface. This optional number indicates the importance of the data for server operation, with 1 being most important and 0 being least important.

```typescript
priority?: number;
```