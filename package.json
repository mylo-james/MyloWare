{
  "name": "myloware",
  "version": "1.0.0",
  "private": true,
  "description": "MyloWare - AI-powered workflow automation platform",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "dev": "docker-compose up -d",
    "dev:down": "docker-compose down",
    "clean": "npm run clean --workspaces",
    "prepare": "husky install",
    "pre-commit": "lint-staged",
    "pre-push": "npm run format:check && npm run build && npm run test",
    "hitl:success": "node scripts/hitl-notify.js --priority=1 --title=\"MyloWare Workflow Success\"",
    "hitl:failure": "node scripts/hitl-notify.js --priority=2 --title=\"MyloWare Workflow Failure\"",
    "build:watch": "tsc --watch",
    "dev:watch": "nodemon --watch src --ext ts,js --exec \"npm run build\"",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:seed": "tsx prisma/seed.ts",
    "db:reset": "prisma migrate reset --force",
    "db:studio": "prisma studio",
    "db:validate": "tsx scripts/validate-schema.ts",
    "db:test": "tsx scripts/test-schema.ts"
  },
  "devDependencies": {
    "@prisma/client": "^5.7.0",
    "@types/node": "^20.11.0",
    "@typescript-eslint/eslint-plugin": "^6.18.1",
    "@typescript-eslint/parser": "^6.18.1",
    "eslint": "8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "husky": "^8.0.3",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.4",
    "prisma": "^5.7.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.4",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  },
  "keywords": [
    "ai",
    "workflow",
    "automation",
    "temporal",
    "nestjs",
    "postgresql",
    "redis"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/myloware/myloware.git"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
