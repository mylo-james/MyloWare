# Local Cloudflare Tunnel config for developer use
# Uses local paths so it can be run without root.
tunnel: mcp-vector
credentials-file: /Users/mjames/Code/mcp-prompts/cloudflared/credentials/mcp-vector.json
origincert: /Users/mjames/Code/mcp-prompts/cloudflared/cert.pem

ingress:
  - hostname: mcp-vector.mjames.dev
    service: http://localhost:3456
  - hostname: n8n.mjames.dev
    service: http://localhost:5678
  - service: http_status:404
